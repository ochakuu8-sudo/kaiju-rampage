# 怪獣ランページ — Stage 1 街レイアウト v6.3 SemanticCluster 反映版

<aside>
📋

**このドキュメントの位置づけ（v6.3 SemanticCluster 反映版）**

Stage 1 街レイアウト再設計の **設計思想 + 詳細フェーズの設計ガイド** をまとめた版。§1〜§7 が方針、**§5 が道路設計ポリシー（中央 `_AVE` のみ固定、それ以外はチャンクごとに都度設計）**、**§6.5 が 実 union 名の確認表**、§6.6（NEW）が **SemanticCluster による意味付けレイヤーの説明**、§9 が全 12 チャンク（Ch0〜Ch11）の参考レイアウト。§9 の TS スケルトンは **「コンセプトに沿った設計の例示」** として扱い、実装時は §5 のポリシーに従ってチャンクのコンセプトから道路を設計し直してよい。

実装の `src/stages.ts` には **SemanticCluster** が既に組み込まれており、各 hero focal の周辺で環境パーティクル（蒸気・桜・水・砂塵・ホタル）が常時 emit される。指示書はこれを前提に読むこと。

</aside>

<aside>
🆕

**v6.2 → v6.3 の変更点（SemanticCluster 反映 + ambient particle）**

- **配置の意味付けレイヤーを実装**: `RawChunkBody.clusters?: SemanticCluster[]` を追加。各クラスタは `{ id, role: 'hero'|'ambient', cell: 'NW'|'NE'|'SW'|'SE'|'merged', focal: BRef|FRef, companions?, boundary?, access?, livingTrace? }` で §3.6 ヒーロー/アンビエント階層と §4 公園モデル 4 層構造をデータ化。Ref はインデックス参照（`{ kind: 'b'|'f', i: number }`）で既存 `buildings/furniture` 配列を非破壊で参照する。
- **Stage 1 全 12 チャンクをクラスタ化済み**: Ch0〜Ch11 すべてに 1 hero + 0-3 ambient のクラスタを宣言。指示書 §3.6 のジグザグ流れ（Ch0 NW → Ch1 SE → Ch2 merged → ...）が型に反映された。
- **自動リンタを追加**: `npm run lint:placement` で 9 ルール (L0-L9) を実行。L0 Ref 解決 / L2 hero companions 数 / L3 livingTrace 各セル必須 / L4 行進検出 / L5 鏡像対称検出 / L6 同サイズ隣接 / L7 孤立電柱 / L8 hero 隣接禁止 / L9 facade 線逸脱 を機械的にチェック。Ch1 児童公園はパイロット実装済みで全ルール Pass。
- **環境パーティクル emit**: 各 hero クラスタの focal 周辺で常時 ambient particle（蒸気・桜・水・砂塵・ホタル）が低レートで emit される。`kominka`→蒸気、`koi_pond`→水、`sakura_tree`→桜、`statue`→ホタル など focal の種別から自動派生。街が「動く」ことで一段抜けたクオリティに。
- **新ヘルパー DSL**: `$B / $F / _CLUSTER / _ROW` を `stages.ts` に追加。`$B/$F` は既存 `_B/_F` と互換で、配列に push しつつ Ref を返す。クラスタを書きたいときに使う、それ以外は従来の `_B/_F` で良い。
- **指示書 §9 のスケルトンは引き続き「例示」**: v6.2 で確立した「実装が正本、指示書は方針提示」の方針を維持。クラスタ宣言は実装側で書く。

**v6.1 → v6.2 の変更点（道路設計ポリシー改訂）**

- **道路は中央 `_AVE` のみ固定**: 縦軸の中央 avenue（x=0）だけが全チャンク共通の固定要素。それ以外（横軸 `_MID_HR` を含むすべての水平道路、`_AVE` 以外の縦道路）は **チャンクのコンセプトに沿って都度設計** する方針へ改訂。
- **§5 を全面書き直し**: 「全チャンクに `_MID_HR` を必ず置く」前提を撤廃。横道路の有無・位置・幅・分割はチャンクごとの物語（住宅街・公園・銭湯・駅前・畑・踏切など）から導出する。`_MID_HR` は便利な「中央クロスのプリセット」であり、義務ではない。
- **§9 の道路指定を「例示」化**: 各チャンクの道路記述は「このコンセプトに対する一案」として残す。実装時はチャンクの焦点・動線・アンビエントから道路を再設計してよい。
- **設計手順の明文化**: チャンク道路は ① コンセプト → ② 動線（誰がどこへ移動するか）→ ③ 焦点エリアの境界 → ④ 道路セグメント、の順で決める。「とりあえず `_MID_HR` を置く」は禁止。

**v6.0 → v6.1 の変更点（履歴）**

- §6.5 新設（型名置換表）。§4.8「向きの意図」改訂（`facing` 未実装に伴う制約整理）。Ch1 を見本チャンクに昇格。道路指示を main ブランチ準拠の `horizontalRoads` / `verticalRoads` 形式に統一。
</aside>

<aside>
⚠️

**v1-v4 撤回のお知らせ**

以前の版で記述していた「鳥居・神社参道・城・城下町・祭り・駅前広場・ダウンタウン・五重塔」等は `src/stages.ts` を確認せず創作したもので、Stage 1 には一切含まれません。v5 では全て撤回し、コードの実構成から書き直しています。

</aside>

<aside>
✅

**整合済みルール**

本ドキュメントでは、Stage 1 の道路は `horizontalRoads` / `verticalRoads` 形式、**縦軸の固定要素は `_AVE`（x=0）のみ**、型名は実 union 名に統一する。`_MID_HR` を含む他の道路はチャンクごとに都度設計する。§9 の TS スケルトンも、道路部分はチャンクのコンセプトに沿った **例示** として読むこと。旧 `roads: [...]` / `_SPINE_V` / 未置換型名は使わない。

</aside>

# §1 Stage 1 の実コンセプト（コードから抽出）

`src/stages.ts` の `STAGE_1_TEMPLATES` より抽出。

## 全体像

- タイトル: **住宅街から街はずれへ**
- 12 チャンク（北→南、y=0 → 2400）
- プレイヤーは怪獣として街を横断する
- チャンク高さ = 200（ローカル dy ∈ [0, 200]）
- 世界幅 X ∈ [-180, +180]

## 4 Acts

| Act | チャンク | テーマ | 既存シグネチャ要素 |
| --- | --- | --- | --- |
| Act I | Ch0-2 | 閑静な住宅街 | 日本庭園 / 児童公園 / 保育園+診療所+郵便局 |
| Act II | Ch3-5 | 生活と小商店 | 朝のカフェテラス / 銭湯の煙突 / civic plaza |
| Act III | Ch6-8 | ローカル商店街 | 小学校キャンパス / 提灯アーケード / 地方の小駅 |
| Act IV | Ch9-11 | 街はずれ | 田園農家+サイロ / 倉庫+火の見やぐら / 踏切 |

## 既存の連続軸

- 主要通り x=0 のファサード連続
- 桜並木 Ch0-5 → 街路樹 `tree` Ch6-11（切替、Ch9-11 は `pine_tree`）
- 電柱+電線を奥層 y=170-195 に散らす
- 人流グラデ（老人・園児 → 買い物客 → 小学生・通勤客 → 疎・野良猫）
- **縦道路で固定するのは中央 `_AVE`（x=0 avenue）のみ**。旧 `_SPINE_V`（x=-90 / 0 / +90）は撤廃。`_AVE` 以外の縦道路はチャンクのコンセプトに沿って都度設計する
- **横道路は固定しない**。`_MID_HR`（y=100 全幅）は便利なプリセットとして提供されるが、義務ではなく、必要なチャンクだけが採用する。横道路の有無・位置・分割・幅は各チャンクの物語（焦点エリアの境界、人の動線、Act の性格）から導く

---

# §2 設計の階層

再配置は **3 階層** で進める。上位から下位へ。下位は上位に従属する。

## 階層 1 — ステージ全体のエリア配置

Act の順序と性格、Act 間の連続軸、ランドマークの配置リズム、ヒーロー/アンビエントのジグザグ流れ（§3.6）。**「12 チャンクを 1 本の物語として読ませる」** レベル。

## 階層 2 — エリア内の建物配置（公園モデル）

セル/エリア単位で、焦点建物とその取り巻き家具を **意味のあるミニチュア** として配置する。

<aside>
🎠

**公園モデル（基本の型、Msg 28 由来）**

公園というエリアを作るなら:

- **焦点**: 中央に遊具（複合遊具+ブランコ+砂場+すべり台）をまとめて置く
- **取り巻き**: その周りにベンチが綺麗に並ぶ（保護者の視線方向）
- **境界**: プランター列や生垣で公園の輪郭を作る
- **動線**: 入口に街灯・花壇、歩道が接続する

この「塊としてエリアが読める」感を、保育園・銭湯・校庭・civic plaza・田の字農地など **すべてのランドマーク** に適用する。

</aside>

## 階層 3 — 道路の多様性

全チャンク同じ道路パターンではなく、**Stage 1 では中央 `_AVE`（x=0）だけを共通の固定軸**とし、それ以外の道路（横軸 `_MID_HR` を含む）はチャンクのコンセプトに沿って都度設計する。Act ごと・チャンクごとに「なぜそこに道があるか」を物語から導き、裏道・アクセス道・搬入路・踏切などを差し込んで性格を変える。横断的要件。

---

# §3 階層 1 — Act レベル方針

## Act I — 閑静な住宅街（Ch0-2）

- 詳細
    
    **時間帯**: 早朝〜朝（街灯は消えている想定、朝刊配達直後、影が東斜めに長い）
    
    **性格**: 朝の空気、老人と園児、庭の手入れ、緑の余白。怪獣の足音が静寂を破る序章。
    
    **密度下限目安**: 建物 18+ / 家具 60+ / humans 5+（**上限なし**。実装後の密度・可読性・処理負荷・当たり判定を見ながら増やしてよい）
    
    **地面基調**: `residential_tile`（温かいベージュ）+ `wood_deck`（縁側）+ `dirt`（菜園）+ `grass` + `stone_pavement`（歩道 x=-65 連続）
    
    **ランドマーク（公園モデル完成候補）**:
    
    - Ch0: 日本庭園（`koi_pond` + `stepping_stones` + `bonsai` + `pine_tree` + `cat` + 縁側 `wood_deck`）
    - Ch1: 町内児童公園（`play_structure` 中央 + `swing_set` + `slide` + `sandbox` + ベンチ対）
    - Ch2: 公共 3 連（`daycare` 園庭 + `clinic` + `post_office`、`pedestrian_bridge`）
    
    **Act II への handoff**: Ch2 下段の地面を `concrete` に切替、桜並木が Ch5 で終端する予告。
    

## Act II — 生活と小商店（Ch3-5）

- 詳細
    
    **時間帯**: 朝〜昼（カフェテラスは朝の営業、銭湯は午後開店、商店全開、パラソル展開、日向の影は短め）
    
    **性格**: 住宅街にじわじわ小商店が増え、子連れ・買い物客の気配が混ざる昼。桜並木終端。
    
    **密度下限目安**: 建物 20+ / 家具 70+ / humans 8+（**上限なし**。実装後の密度・可読性・処理負荷・当たり判定を見ながら増やしてよい）
    
    **地面基調**: `residential_tile` + `concrete` 混在 → Ch5 で `stone_pavement`（civic 格式）。小商店前は `wood_deck`（bakery/bookstore/cafe）。
    
    **ランドマーク（公園モデル完成候補）**:
    
    - Ch3: 朝のカフェテラス（`bakery` + `bookstore` + `cafe` 3 連テラス、`parasol` 4 連 + ベンチ 3 連）
    - Ch4: 銭湯セット（`bathhouse_chimney` + `noren` + `milk_crate_stack` + 常連ベンチ）+ スーパー前買い物客
    - Ch5: civic plaza（`post_office` × `bank` の間の `plaza_tile_circle` + `statue` + `fountain` + `flag_pole` × 2）
    
    **Act III への handoff**: 桜並木が Ch5 で終端、Ch6 から `tree` に切替。小商店密度がじわじわ上がる。
    

## Act III — ローカル商店街（Ch6-8）

- 詳細
    
    **時間帯**: 午後〜夕方前（下校時間、帰宅ラッシュ直前、`chouchin` はまだ点灯前の状態で配置）
    
    **性格**: 下校の小学生と帰宅通勤者で最も賑やか。Stage 1 の密度ピーク。
    
    **密度下限目安**: 建物 22+ / 家具 80+ / humans 12+（**上限なし**。実装後の密度・可読性・処理負荷・当たり判定を見ながら増やしてよい）
    
    **地面基調**: `concrete` ベース + 校庭 `dirt`（大） + 商店前 `wood_deck` + 駅前 `asphalt` / `stone_pavement`
    
    **ランドマーク（公園モデル完成候補）**:
    
    - Ch6: 小学校キャンパス（校門 `flag_pole` × 2 + `statue` 二宮像 + フル遊具スイート + プール + 裏山松林）
    - Ch7: 提灯アーケード（avenue 両脇 `chouchin` 14 本 + `banner_pole` × 4）
    - Ch8: 地方の小駅（`train_station` + 駅前小広場 + `taxi_rank_sign` + `bus_stop`）
    
    **Act IV への handoff**: Ch8 下段で住宅が増え、`pine_tree` が登場して郊外感を予告。
    

## Act IV — 街はずれ（Ch9-11）

- 詳細
    
    **時間帯**: 夕方〜夜の入口（`gas_station` / `ramen` / `signal_tower` / 点灯 `chouchin` が光源、`puddle_reflection` で湿り気）
    
    **性格**: 商店街を抜けた郊外。畑と倉庫、寂寥感、夜の気配。Ch11 で踏切を越えて Stage 2 へ。
    
    **密度下限目安**: 建物 16+ / 家具 55+ / humans 5+（**上限なし**。実装後の密度・可読性・処理負荷・当たり判定を見ながら増やしてよい）
    
    **地面基調**: `grass`（畑周辺） + `dirt`（畑） + `asphalt`（倉庫・踏切）に徐々にシフト。`puddle_reflection` の湿り気。
    
    **ランドマーク（公園モデル完成候補）**:
    
    - Ch9: 田園農家（`grain_silo` + ビニールハウス群 + 畑の水たまり + 野良猫）
    - Ch10: 郊外倉庫 + 消防分署（`warehouse` + `fire_watchtower` + `police_station`）
    - Ch11: 踏切 + 信号塔 × 2（深夜営業 `gas_station` + 24h `convenience` + `ramen` の夜ネオン予感）
    
    **Stage 2 への handoff**: Ch11 の踏切で完全分断、`puddle_reflection` + `chouchin` で夜街 Stage 2 の灯りを予告。
    

## Act 間の連続軸サマリ

| 連続軸 | Act I | Act II | Act III | Act IV |
| --- | --- | --- | --- | --- |
| 歩道 | stone_pavement x=-65 | wood_deck パッチ | tile / asphalt パッチ | なし・路肩のみ |
| 人流 | 老人・園児 | 買い物客 | 小学生・通勤客 | 疎・野良猫 |
| 象徴猫 | 縁側・庭園 | テラス・店裏 | 路地裏 | 畑・店裏 |

---

# §3.5 セル分割の基本グリッド

各チャンクは **2×2 の基本セル**に分割する。各セルが公園モデル（焦点+取り巻き+境界+動線）の最小単位となる。

## セル座標

| セル | x 範囲 | y 範囲（ローカル） | facade 線 |
| --- | --- | --- | --- |
| NW | -180 〜 0 | 0 〜 100 | y=22（上段） |
| NE | 0 〜 +180 | 0 〜 100 | y=22（上段） |
| SW | -180 〜 0 | 100 〜 200 | y=118（下段） |
| SE | 0 〜 +180 | 100 〜 200 | y=118（下段） |
- 各セルは **約 180×100** の矩形
- セル境界は論理上、中央 `_AVE`（x=0 の avenue）と **論理線 y=100**（道路があってもなくても境界として扱う）で区切る。`_MID_HR` を必ず置く必要はなく、チャンクのコンセプトに応じて道路を引かない選択もとれる。旧 `_SPINE_V` の x=-90 / +90 側道は Stage 1 では固定骨格として扱わない
- 上段セル（NW/NE）は facade 線 y=22、下段セル（SW/SE）は y=118

## セルマージ（焦点が大きい場合）

焦点が単一セルに収まらない場合、隣接セルをマージできる。マージ後のセルは 1 つの焦点+取り巻きセットで構成する。

- **NW+NE マージ**: 上段全幅（幅 360、高さ 100）— 商店街アーケード（Ch7）、田園上段（Ch9）
- **SW+SE マージ**: 下段全幅（幅 360、高さ 100）— civic plaza（Ch5）、駅前広場（Ch8）
- **NW+SW マージ**: 左半全高（幅 180、高さ 200）— 校庭や畑など左半分を占める大きな屋外焦点
- **全セルマージ**: 小学校キャンパス（Ch6、校門+校舎+校庭+プール+裏山が全体を占める）

単一セルからスタートし、**焦点が溢れるときだけ**マージする。最初からマージ前提で設計しない。

## セルごとの密度配分

Act の密度下限目安をセル単位に割る。マージセルは割当の合算を**最低ライン**として扱う。

- **建物**: 各セル 1-2 棟を出発点にするが、**上限は設けない**。マージ時や基本形 C クラスターでは、焦点性・読みやすさ・当たり判定が破綻しない範囲で必要な棟数まで増やしてよい。焦点役割のない背景埋め建物は置かない
- **家具**: 各セル 12 個以上を目安にし、**上限は設けない**。ミニチュア感の密度は家具で作る。賑わいセルでは 35 個を超えてもよい
- **人**: 各セル 0 人以上。全セル均等ではなく、賑わいセルに寄せる。上限は設けず、活動が読める範囲で増やしてよい

セル単位で密度を配分することで、「意味のない建物で埋まった区画」を作らず、賑わいは家具の密度で表現する。

---

# §3.6 ヒーロー / アンビエント階層（チャンク内）

1 チャンクの 4 セル（または全マージ）を全て同等に扱わない。**視覚の山と谷** を作る。

## 2 種類のセル役割

- **ヒーローセル**: チャンク内で **1-2 セル**。§4 の 4 層構造を完備し、公園モデル基本形 A/B/C を適用した「視線が止まる焦点」を持つ
- **アンビエントセル**: 残り 2-3 セル。住宅・路地・空き地・小さな植栽で構成し、焦点を持たない。家具密度は保つ（§3.5 下限以上）。建物は 0-1 棟を基本とするが、密度や読みやすさが不足する場合は、焦点化しない脇役住宅・小屋・店裏要素として追加してよい

## 配置ルール

- **ヒーロー隣接禁止**: ヒーローセル同士を avenue (x=0) / 論理線 y=100 両側で隣接させない（2 ヒーロー時は対角配置 = NW+SE または NE+SW）。境界線として扱うのは「道路があるか」ではなく「セルの論理境界」
- **ジグザグ流れ**: 隣接チャンク（北側 / 南側）のヒーロー位置と被らないように、Stage 全体で稲妻型の流れを作る（例: Ch3 NE ヒーロー → Ch4 SW ヒーロー → Ch5 SW+SE マージ → Ch6 全マージ → Ch7 NW+NE マージ）
- **マージチャンクの扱い**: 全マージ（Ch6 小学校）や上段マージ（Ch7 商店街 = NW+NE）/ 下段マージ（Ch5 civic plaza = SW+SE）は「1 大ヒーロー」としてカウント。マージチャンクはアンビエントセルを持たない（例外として OK）
- **Act 境界での連続許容**: Ch5（Act II 末）→ Ch6（Act III 頭）→ Ch7 の 3 連続大ヒーローは **Stage 1 のクライマックス** として許容。ただし、Act 境界 **以外** では大ヒーローチャンクを 3 連続させない（情報過多になる）

## 効果

チャンクをスクロールするとヒーローが交互に現れ、アンビエントセルが呼吸を作る。全セル焦点は「情報過多の地図」になりミニチュア感を失う。**ヒーローが主役、アンビエントが脇役** の役割分担を守る。

---

# §4 階層 2 — エリア内配置の原則（公園モデル）

## 建物と家具の扱いの違い

<aside>
⚖️

**「詰め込み」の基準は建物数であって家具数ではない**

- **建物は役割のあるものだけ**: 焦点、または焦点クラスター（基本形 C）の一員として意味が明確な建物だけを置く。「このチャンクが寂しいから建物を足す」という判断はしない。棟数の上限は設けず、焦点性・読みやすさ・当たり判定が破綻しない範囲で必要なだけ増やしてよい
- **家具は密度を恐れない**: ミニチュア感は家具の数で作られる。Act 密度目安の家具数は下限として扱い、上限は設けない。賑わいセルでは必要なだけ増やしてよい
- **「スカスカ」はまず家具で埋める**: 建物数の上限は撤廃するが、空間を埋める第一手は家具にする。ベンチ・プランター・自販機・街灯・物干し・エアコン室外機・看板等で密度を上げ、それでも焦点性が必要な場合のみ建物を足す
- **識別性テスト（§4.5 由来）を最優先**: 建物が少なくても取り巻きで建物種が読めれば OK。建物を増やして説明しない
</aside>

## 4 層構造

全セル/エリアは、可能な限り以下の 4 層で構成する。

<aside>
🎯

**焦点 → 取り巻き → 境界 → 動線**

- **焦点 (focal)**: 1-2 棟の建物、または公園/広場/校庭など「無建物エリア」。セルの中心に置く。
- **取り巻き (satellites)**: 焦点に帰属する家具群（看板 / 駐輪 / ベンチ / プランター / 自販機 / 物干し / エアコン室外機 / 煙突 等）。焦点の周囲 10-20 px に集めて「塊」を作る。
- **境界 (boundary)**: セルの輪郭を描く要素（wood_fence / hedge / flower_bed / flower_planter_row / power_pole 等）。セル端 x=±178 付近、y=22 または y=118 の facade 線。
- **動線 (access)**: 歩道の grounds タイル、街灯、マンホール、バス停 等。主要通り（x=0 の `_AVE`）と、**そのチャンクが採用する横道路**（採用していれば `_MID_HR` 相当の y=100 帯、または独自配置の `_HR(...)`）に沿う。横道路を持たないチャンクでは、動線家具は焦点と歩道帯（x=-65 付近）に寄せる。
</aside>

## ミニチュアの 3 つの基本形

- 基本形 A: 公園型（オープンスペース焦点）
    - **焦点**: 遊具/彫像/噴水などの無建物焦点を中央に集約
    - **取り巻き**: 対称または準対称のベンチ + プランター
    - **境界**: hedge / flower_bed / 路面タイル切替
    - **例**: Ch1 児童公園、Ch5 civic plaza、Ch6 校庭
- 基本形 B: 店舗型（単一建物焦点）
    - **焦点**: 1 棟を標準とする商店または公共施設。密度や識別性が不足する場合は、同じ役割の小規模な付属棟を追加してよい
    - **取り巻き**: `a_frame_sign` + `shop_awning` + 駐輪 + パラソル + 自販機（店種による）
    - **境界**: `flower_planter_row` + `potted_plant`
    - **例**: Ch3 の bakery/bookstore/cafe、Ch8 の train_station
- 基本形 C: クラスター型（複数建物連結）
    - **焦点**: 2-4 棟を標準とする同ジャンル建物の並び。密度・可読性・当たり判定が破綻しない範囲で、必要なら 5 棟以上へ拡張してよい
    - **取り巻き**: 共通の `shop_awning` 列 / `parasol` 連 / 共有ベンチ列
    - **境界**: 両端の `power_pole` + hedge、連なる facade
    - **例**: Ch3 の 3 連テラス、Ch7 の商店街、Ch9 の温室群、Ch10 の倉庫 2 連

## 既存チャンクへの公園モデル適用マップ

現状の各ランドマークに公園モデルを適用した場合の焦点・取り巻き・境界・動線の割り振り。

| エリア | 基本形 | 焦点 | 取り巻き（完成に必要な追加候補を含む） | 境界 | 動線 |
| --- | --- | --- | --- | --- | --- |
| Ch1 児童公園 | A | play_structure + swing_set + slide + sandbox | bench × 2（保護者）+ flower_planter_row + flower_bed + street_lamp × 2 + sakura_tree（奥） | tile 公園敷石 + hedge（追加候補） | 公園入口 → avenue 接続 |
| Ch3 3 連テラス | C | bakery + bookstore + cafe | parasol × 4 + bench × 3 + flower_planter_row × 2 + shop_awning × 3 + bicycle_row + a_frame_sign × 3 | 両端 power_pole + wood_deck 舗装の端 | wood_deck テラス広場 |
| Ch5 civic plaza | A | statue + plaza_tile_circle | fountain + flag_pole × 2 + bench × 2（対）+ flower_bed × 2（対）+ street_lamp × 2 + flower_planter_row | plaza_tile_circle の輪郭 | stone_pavement 中央 + avenue 入口 |
| Ch7 商店街 | C | chouchin 14 本 + banner_pole × 4 | 各店舗 a_frame_sign + shop_awning + parasol + bicycle_rack | facade 両端 power_pole + wood_fence | avenue 天蓋 + 各店前 wood_deck/grass/tile パッチ |
| Ch9 田の字農地 | A | dirt 畑 4 区画 + grain_silo | greenhouse × 複数 + flower_planter_row（苗）+ puddle_reflection + 畦道フェンス | wood_fence 田の字の外周 | grass 畦道（追加候補） |
| Ch11 踏切 | A | railroad_crossing + signal_tower × 2 | traffic_cone × 2 + puddle_reflection × 2 + cable_junction_box × 2 + telephone_booth | power_line × 4 + power_pole 両端 | asphalt 全面 |

## 左右非対称の原則

- **奇数個で構成する**（benches × 3、planters × 3 等）
- 対の要素を置くときも、わずかに X をずらす（例: statue は x=0 ではなく x=2）
- 家具の「行進」（mailbox が等間隔で一列）は Ch0 で既に 4 個に削減済み。**他チャンクにも同様に適用**
- `_F` 呼び出しは可能な限りクラスタごとにコメント区切り、一列に見えないようにする

## 建物タイプ別の取り巻き辞書

Stage 1 で使用される各建物タイプに対し、**らしさを作る署名取り巻きセット**を固定する。詳細フェーズではこの辞書から引いて配置する。必須要素は各建物に必ず付ける。任意要素はセルの余裕に応じて追加。

| 建物タイプ | 基本形 | 必須取り巻き | 任意取り巻き |
| --- | --- | --- | --- |
| bakery | B/C | shop_awning + a_frame_sign + parasol × 2 | flower_planter_row / bicycle_row / cat |
| bookstore | B/C | shop_awning + a_frame_sign + bench | potted_plant / bicycle_rack |
| cafe | B/C | shop_awning + parasol × 2 + bench | flower_planter_row / a_frame_sign / cat |
| bathhouse | B | bathhouse_chimney + noren + milk_crate_stack | bench（常連席）/ cat / 自販機 |
| post_office | B | mailbox + flag_pole + bench | flower_planter_row / bicycle_rack |
| bank | B | flag_pole + potted_plant × 2 + bench | plaza_tile_circle 前面 |
| clinic | B | bench + potted_plant × 2 + a_frame_sign（診療時間） | flower_planter_row |
| daycare | A（園庭含む） | swing_set + slide + sandbox + flower_bed | bench（保護者待機）/ play_structure 小 |
| school | A（校庭含む） | flag_pole × 2（校門）+ statue（二宮像）+ play_structure + swing_set + slide + sandbox + dirt 大パッチ（校庭） | koi_pond（プール代用）/ pine_tree 裏山 |
| train_station | B | taxi_rank_sign + bus_stop + bench × 2 + 駅前 asphalt パッチ | newspaper_stand / 自販機 / flower_planter_row |
| convenience | B | a_frame_sign + bicycle_rack + 自販機 | traffic_cone / 深夜客 |
| ramen | B | chouchin × 2 + noren + a_frame_sign | bicycle_rack / 自販機 |
| gas_station | B | traffic_cone × 3 + 自販機 + asphalt 大パッチ | puddle_reflection / signal_tower |
| warehouse | B/C | power_pole + puddle_reflection + asphalt | traffic_cone / cable_junction_box |
| fire_station | B | fire_watchtower + flag_pole + traffic_cone × 2 | bench / signal_tower |
| police_station | B | flag_pole + bench + potted_plant | traffic_cone |
| grain_silo（+ 農家セット） | A/C | greenhouse × 2+ + dirt 畑区画 + wood_fence（畦） | puddle_reflection / cat / 農機具家具 |

**適用原則**:

- 焦点 1 棟 = 必須取り巻きをセル内に全配置。任意取り巻きはセルの余裕次第で追加
- クラスター型（C）で複数建物を並べる場合、必須取り巻きは共有可（例: 3 連テラスの `shop_awning` は 3 棟連続で 1 つの帯として扱う）
- **識別性テスト**: セルから建物を除いたとき、取り巻きだけで建物種が読めるか？（例: `bathhouse_chimney` + `noren` があれば銭湯と即分かる）— 読めなければ必須取り巻きを追加する
- 辞書にない建物タイプが必要になったら、詳細フェーズに入る前にこの表へ追記してから使う

## 人流の配置ルール

Act 別の人流グラデ（§3 連続軸サマリ）を、セル内のどこに置くかの原則。

| 状況 | 配置先 |
| --- | --- |
| 店前の客 | 焦点建物の facade 線に接する位置（y=30-35 または y=125-130） |
| ベンチ休憩 | ベンチの隣 ±3 px 以内 |
| 歩行者 | 歩道 grounds 上（x=-65 付近 stone_pavement、または **そのチャンクが横道路を採用している場合** その脇 y=92/108） |
| 園児・小学生 | 園庭/校庭の dirt パッチ内、または公園焦点の周囲 |
| 野良猫 | 店裏・縁側・畦道・畑・塀際（境界要素の近く、奇数個で不均等） |
| 老人 | 縁側 wood_deck 上、または公園ベンチ |
| 通勤客・学生の群れ | 駅前広場・`_AVE` 中央歩道帯に束で配置（2-3 人の小集合） |
- 人は**焦点か取り巻きに帰属する**形で置く（道路中央に単独の人を置かない）
- 人数は Act 密度下限目安を下回らないようにし、**賑わいセルに寄せる**（全セル均等は不自然）。上限は設けず、活動が読める範囲で増やしてよい
- セル内の人配置がそのセルの焦点の「活動」を語るか？（保育園セル → 園児、銭湯セル → 常連老人、駅前セル → 通勤客）

## 取り巻き配置の 3 パターン

§4.5 辞書で選んだ取り巻きを**どう並べるか**の型。1 つの焦点に対し、取り巻きを以下 3 パターンに分類して配置する。

| パターン | 適用する家具 | 配置ルール |
| --- | --- | --- |
| facade 沿い型 | shop_awning / a_frame_sign / noren / chouchin / banner_pole / mailbox | facade 線（上段 y=22 / 下段 y=118）に貼り付け。shop_awning は建物の dx と同じ x、a_frame_sign は店前 ±5 px。左右はゆるく対称（±X シフト 2-5 px） |
| 焦点取り囲み型 | bench / parasol / flower_bed / flower_planter_row / potted_plant / street_lamp / statue / fountain | 焦点から半径 15-30 px の弧上に配置。奇数個（3 / 5）で準対称、対なら X を 2-5 px ずらす。基本形 A（公園型）で主に使用 |
| 歩道沿い型 | 自販機 / bicycle_rack / bicycle_row / traffic_cone / newspaper_stand / bus_stop / taxi_rank_sign | 歩道帯に列で配置。チャンクが横道路を持つならその脇（例: y=92 / y=108）、x=-65 歩道帯なら x=-60〜-70 の帯。間隔 8-15 px で不均等に |

**適用の優先順**:

1. 署名的取り巻き（`bathhouse_chimney` / `flag_pole` 等、建物アイデンティティを作る物）は **facade 沿い型**
2. 休憩・装飾（`bench` / `parasol` / `flower_bed`）は **焦点取り囲み型**
3. 機能家具（自販機 / 駐輪 / 案内）は **歩道沿い型**

**3 連店舗（基本形 C）の特例**: `shop_awning` は 3 棟連続で 1 つの帯として扱う（個別のファサード付けではなく共通庇）。`parasol` も 3 店舗前で 1 列に並べる。

**「綺麗に並ぶ」 vs 「左右非対称」の両立**:

- パターン単位では綺麗に並べる（bench 3 連は等間隔 + 弧形）
- パターン間で X を 2-5 px ずつずらす（bench 列と parasol 列は同じ x 中心に揃えない）
- 焦点自体も x=0 ではなく x=±2〜5 にずらす

→ ミクロで整列、マクロで不均等、の二層で解決する。

## スケール階層と生活の痕跡

### スケール階層（3 段階の大きさ差）

1 セル内で **大 / 中 / 小** の 3 段階サイズ差を作る。深さと重みを生む。

- **大**: 焦点建物 or オープンスペース焦点（school / grain_silo / 遊具群 / plaza_tile_circle 大パッチ）
- **中**: サテライト構造物（`bathhouse_chimney` / `flag_pole` / `statue` / 大型 `parasol` / `greenhouse` / `fire_watchtower`）
- **小**: 家具（`bench` / `potted_plant` / `a_frame_sign` / `mailbox` / 自販機 / `cat` / `traffic_cone`）

**目安比**: 大 1 : 中 2-3 : 小 10-20。同サイズ要素ばかりが並ぶとミニチュア感が死ぬ。

### 生活の痕跡（"ついさっきまで誰かがいた" 感）

各セルに **1 個以上** 生活感の痕跡要素を置く。機能ではなく雰囲気の要素。

- `laundry_pole` / `laundry_balcony`（物干し。風に揺れる想定はコメントで表現）
- `bicycle_row` / `bicycle_rack` の一台だけ微妙に x/y をずらす（停め方の乱れ）
- `_G('fallen_leaves', x, y, w, h)` 小パッチ（季節感、掃き残し）
- `potted_plant` / `flower_planter_row` の微妙な不揃い（置き主の性格）
- `mailbox` の flag_up ニュアンス（郵便が届いた直後、コメントで意図記述）
- half-open `noren` / `banner` の揺れ（コメントで表現）
- `garbage` / `recycling_bin`（生活ゴミ出し）
- `cat`（既ルール、1 チャンク 1-3 匹、焦点の脇役スポットに）

痕跡がゼロのセルは「ゲームの背景」でしかなく、ミニチュアの愛着は生まれない。

### 向きの意図（Orientation）— v6.1 改訂

<aside>
⚠️

**v6.1 改訂**: 家具の向き仕様は現行エンジンで **一部しか表現できない** ため、「設計意図」と「実装可能部分」を分けて記述する。以前の v6.0 版で挙げていた向きルールのうち、位置で表現できるものだけが実装可能。

</aside>

**現状のエンジン制約**: `FurnitureItem` には `facing` フィールドが存在せず（`{ type, x, y, hp, active, score, lightTimer, lightState, chunkId }`）、`fillInstances` も `type + x + y` のみで描画する。スプライトは常に固定向きで描かれる。

**表現可能なこと（位置で意図を示す）**:

- **ベンチ**: 焦点のすぐ手前 (5-15 px) に置く → 「焦点を眺める配置」として読める（公園ベンチは遊具手前、civic plaza ベンチは `statue` 手前、銭湯ベンチは玄関手前）
- **自販機 (`vending`)**: 建物 facade から 2-3 px 離して置く → 壁背の購入動線が読める
- **物干し (`laundry_pole` / `laundry_balcony`)**: 建物の南側（y 値が大きい側）に置く → 日当たり配置が読める
- **`a_frame_sign` / `noren`**: 建物 facade の道路側に 3-8 px 手前に置く → 客から読める向きが読める
- **`statue`**: 配置 x で向きを示唆（civic plaza の像は avenue 中心 x=0〜+2、校庭の二宮像は校舎側寄り）

**表現できないこと（現状は諦める）**:

- スプライトの回転・左右反転
- 旗の風向き、`noren` の揺れ、`bicycle` の前輪向き、`statue` の顔の向き
- `mailbox` の flag_up 状態、`parasol` の開閉ニュアンス

**TODO（エンジン側改修）**: `FurnitureItem.facing: 'N' | 'E' | 'S' | 'W'` を追加し、`fillInstances` 側で左右反転描画に対応すれば本節の向きルールが完全実装可能になる。優先度 中（Stage 1 の完成度を一段上げる改修）。

**実装者ガイドライン**: 向き指示は `_F` の直後にコメントで残す（例: `_F('bench', 90, 122), // 遊具 (x=90, y=145) を向く`）。facing フィールド追加後にコメントをコードへ反映する。Ch1 見本チャンク（§9）のインラインコメントを参照。

---

# §5 階層 3 — 道路設計ポリシー（中央 `_AVE` のみ固定、他はチャンクごとに都度設計）

<aside>
🛣️

**最重要: Stage 1 の道路で固定するのは中央 `_AVE`（x=0 縦軸）だけ。**

`_AVE = _VR(0, 0, 200, 'avenue')` は全 12 チャンクを貫く唯一の共通縦軸。それ以外の道路（横軸 `_MID_HR` を含むすべての水平道路、`_AVE` 以外の縦道路）は、**チャンクのコンセプトに沿って都度設計** する。「とりあえず `_MID_HR` を置く」「とりあえず `_SPINE_V` を置く」は禁止。道路は装飾ではなく、エリアの物語を語る要素として配置する。

</aside>

## 設計思想

v6.1 以前の指示書は、全 12 チャンクに `_MID_HR` + `_SPINE_V` を一律で置く前提だった。これにより:

- どのチャンクも似た形に見え、Act の性格差が出ない
- 焦点エリア（公園・銭湯・駅前・畑など）の輪郭が道路に潰される
- 「なぜそこに道があるか」が説明できないセグメントが量産される

v6.2 ではこれを撤回し、**「中央 `_AVE` を背骨として、各チャンクが固有の道路レイアウトを持つ」** 方針に統一する。`_MID_HR` は便利なプリセット（中央クロスを置きたいときの近道）として残るが、義務ではない。

## 実装上の道路 API

Stage 1 raw チャンクでは、`raw` 内で以下を使う。

```tsx
horizontalRoads: [...チャンクのコンセプトに沿った水平道路（0〜複数本）],
verticalRoads: [_AVE, ...チャンクのコンセプトに沿った追加縦道路（0〜複数本）],
```

- `_AVE`: 中央 avenue。`_VR(0, 0, 200, 'avenue')`。**Stage 1 で固定の唯一の縦道路**
- `_MID_HR`: 中央クロス街路のプリセット。`_HR(100, -180, 180)`。**便利だが必須ではない**。チャンクが y=100 全幅の横道路を採用すべき場合のみ使う
- `_TOP_HR`: 上端クロス街路のプリセット。`_HR(200, -180, 180)`。Act 境界や接続点が必要な場合のみ使う
- `_HR(dy, xMin, xMax, cls?)`: 部分幅の水平道路。裏道、アクセス道、T字路、搬入路、踏切前の引き込み道などに使う
- `_VR(dx, yMinLocal, yMaxLocal, cls?)`: 部分高さの縦道路。商店街裏、倉庫脇、駅前導線などに使う

## チャンク道路の設計手順（必読）

道路は「先に置く」のではなく「コンセプトから導く」。次の順で決める:

1. **コンセプト**: そのチャンクで何を見せるか（焦点となるエリア、Act の役割）を 1 文で言語化する
2. **動線**: 怪獣プレイヤーから見て、誰が（住人 / 商客 / 通勤客 / 通学者 / 農家）どこから来てどこへ向かうかを描く
3. **焦点の境界**: 焦点エリア（公園 / 銭湯 / civic plaza / 校庭 / 駅前 / 畑 / 踏切）の輪郭をどの線で切るかを決める。輪郭は「道路で切る」「ground パッチで切る」「fence / hedge で切る」の選択肢がある
4. **道路セグメント**: 上記から「絶対に必要な道路」だけを選ぶ。`_AVE` は必ず通る（背骨）。横道路は「なくてもチャンクが読めるか」を自問してから足す

「なんとなく対称になるから」「他のチャンクが置いているから」は道路を足す理由にしない。

## Stage 1 道路の基本方針

- **固定するもの**: 中央 `_AVE`（x=0）**のみ**。全 12 チャンクで `verticalRoads` の先頭に置く
- **固定しないもの**: 横軸（`_MID_HR` を含む）と、`_AVE` 以外の縦道路。すべてチャンクごとに採否・配置・幅・分割を決める
- **道路で埋めない**: 道路を増やす目的は密度ではなく、エリアの読みやすさを上げること。スカスカ感は道路ではなく家具・地面パッチ・境界で埋める
- **広場は道路ではなく ground で作る**: 駅前広場、civic plaza、テラス、校庭は `asphalt` / `stone_pavement` / `tile` / `wood_deck` / `dirt` の `_G` で面を作る。道路セグメントを増やして広場を表現しない
- **道路は建物の説明に使う**: コンビニ前の駐車帯、商店裏の搬入路、公共施設前のアクセス道、倉庫脇のサービス道路、踏切の引き込みなど、「なぜそこに道があるか」が読めること
- **横道路ゼロも許容**: 焦点が縦に通る場合（例: 畑の畦道、参道、踏切）、横道路を 0 本にしてよい。`horizontalRoads: []` は正当な選択

## Act ごとの道路性格（参考）

| Act | 道路性格 | 設計のヒント |
| --- | --- | --- |
| Act I 住宅街 | 静かな生活道路、生活感のあるアクセス | `_AVE` が背骨。横道路は「公園入口」「公共施設前」など意味があるところに 0〜2 本だけ。生活路地は道路ではなく `concrete` / `residential_tile` の ground 帯で表現する選択肢も有効 |
| Act II 生活商店 | 店前の歩道、搬入路、交差点感 | 中央 `_AVE` 必須。商店搬入路として片側縦道（例: `_VR(-130, 0, 100)`）を入れると生活感が出る。広場は道路を増やさず ground で広げる |
| Act III ローカル商店街 | 校門前、アーケード、駅前導線 | アーケードは「道路を増やす」のではなく **avenue 両脇に家具（`chouchin` / `banner_pole` / `shop_awning`）を密に並べる** ことで作る。駅前広場は `asphalt` ground と `bus_stop` / `taxi_rank_sign` の家具で表現 |
| Act IV 街はずれ | 疎らな直線、農道、倉庫脇、踏切 | 道路は最小限。畦道は `_G('dirt')` / `_G('grass')` + `wood_fence` で。倉庫サービス道路は短い `_HR` / `_VR` で必要なら足す。踏切では `railroad_crossing` / `railway_track` / `signal_tower` で `_AVE` を視覚的に分断する（道路は足さない） |

## チャンク別の参考道路（v6.1 互換の例示。コンセプトから再設計してよい）

下表は v6.1 の指示書で挙げられていた道路案を **参考** として残したもの。各チャンクのコンセプトを見直したうえで、必要なら別案に差し替えてよい。`_MID_HR` も「採用しない」選択を含めて検討する。

| Chunk | コンセプト | 参考道路（一案） | 設計時の問い |
| --- | --- | --- | --- |
| Ch0 | 住宅街入口の静けさ・日本庭園 | `verticalRoads: [_AVE]`, `horizontalRoads: [_MID_HR]`（または横道路なし） | 庭の連続感を見せたいなら `_MID_HR` を抜く案もあり |
| Ch1 | 児童公園と周辺住宅 | `_AVE` + 公園前アクセス道（例: `_HR(130, 10, 180)`） | 公園入口の動線をどこに置くか |
| Ch2 | 公共施設 3 連 + Act II 接続 | `_AVE` + 公共施設前アクセス道 + 上端クロス（例: `_TOP_HR`） | Act 境界の予告をどう作るか |
| Ch3 | 朝のカフェテラス 3 連 | `_AVE` + 商店搬入路（例: `_VR(-130, 0, 100)`） | テラス前は道路と歩道どちらで見せるか |
| Ch4 | 銭湯とスーパー | `_AVE` + 銭湯/スーパー前の短い `_HR`（広場感は ground で） | 店前の溜まりは道路の交差点で出すか、wood_deck パッチで出すか |
| Ch5 | civic plaza | `_AVE` のみ（広場は ground と家具） | 広場の輪郭は道路で切らず、`plaza_tile_circle` で見せる |
| Ch6 | 小学校キャンパス | `_AVE` + 校門前アクセス道 + 上端クロス | 校庭は `dirt` ground、校門だけが道路接続点 |
| Ch7 | 提灯アーケード | `_AVE` のみ（アーケードは家具で作る） | 道路を増やさず家具密度で読ませる |
| Ch8 | 地方の小駅 | `_AVE` のみ（駅前広場は `asphalt` ground） | バス・タクシー導線は家具配置で表現 |
| Ch9 | 田園農家と畑 | `_AVE` のみ。横道路 0 本もあり | 畦道は道路ではなく ground と `wood_fence` |
| Ch10 | 倉庫と消防分署 | `_AVE` + 倉庫脇の短い `_VR` / `_HR` | サービス道路は誰のためか（消防出動 / 搬入） |
| Ch11 | 踏切と街はずれの終端 | `_AVE` + `railroad_crossing` / `railway_track` で分断（横道路は最小限） | 踏切は道路を増やすのではなく avenue を切る |

## 道路タイプ語彙

| タイプ | 厚さ h / 幅 w | 用途 | 採否 |
| --- | --- | --- | --- |
| 中央 avenue（`_AVE`） | w=18 | 主要縦軸。Stage 1 の **唯一の固定要素** | **必須（全チャンク）** |
| main horizontal プリセット（`_MID_HR`） | h=14 | 中央水平クロス（y=100、全幅 -180〜180）のショートカット | 採否はチャンクごと |
| top horizontal プリセット（`_TOP_HR`） | h=14 | 上端水平クロス（y=200、全幅）のショートカット | 採否はチャンクごと（Act 境界などで） |
| 部分幅水平道路 | h=10-14 | `_HR(dy, xMin, xMax)`。アクセス道、搬入路、倉庫脇、踏切前 | チャンクごと |
| 部分高さ縦道路 | w=14 / 18 | `_VR(dx, yMinLocal, yMaxLocal)`。商店搬入路、倉庫脇、駅前導線 | チャンクごと |
| 広場 / civic plaza | ─ | 道路ではなく ground + 家具で表現 | 該当チャンクのみ |
| 踏切 | ─ | `railroad_crossing` / `railway_track` / `signal_tower` / `traffic_cone` で表現 | Ch11 |

## 制約

- `verticalRoads` には必ず `_AVE` を入れる（Stage 1 の唯一の固定縦軸）。それ以外は任意
- `horizontalRoads` は **空配列 `[]` も許容**。横道路を持たないチャンク（Ch9 畑など）が成立する
- `_SPINE_V` は使わない（後方互換用、Stage 1 の新規 raw では原則禁止）
- 追加道路は `horizontalRoads` / `verticalRoads` に入れる。`roads: [...]` 形式へ戻さない
- x=-90 / +90 の縦道を追加する場合も `_VR(-90, yMin, yMax)` / `_VR(90, yMin, yMax)` として、**そのチャンク内の意味（裏道 / 搬入路 / 通学路など）をコメントで説明** する。説明できない縦道は置かない
- 道路と建物の接触判定は `placeScene` 側では未管理。手動で `xMin` / `xMax` / `dx` / `dy` を調整し、建物が道路に食い込まないようにする
- ground の `asphalt` は道路そのものではなく、駐車場・駅前広場・倉庫前ヤードにも使う。道路として車線を持たせたい場合だけ `_HR` / `_VR` を使う

## 地面パッチの配分原則

Act 基調（§3）と焦点地面（§4 基本形）が衝突しないよう、以下で配分する。

- **非焦点セル**は Act 基調 grounds（例: Act I は `residential_tile`）のみ。パッチを追加しない。
- **焦点セル**のみパッチを持つ:
    - 基本形 A: `tile` / `plaza_tile_circle` / `dirt`（校庭・畑）など焦点専用地面
    - 基本形 B: `wood_deck` 小パッチ（店前 1 棟分、幅 30-50 px）
    - 基本形 C: `wood_deck` 帯（3 棟連続、幅 80-120 px）
- **パッチのサイズ目安**: 焦点（建物 or オープンスペース）の矩形の **1.2 倍前後**を出発点にする。実装後にスカスカに見える場合は広げてよいが、Act 基調が消えないように境界・濃淡・余白で調整する。
- **歩道帯**（x=-65 の `stone_pavement`）は全チャンク共通で、Act 基調とは別扱い。
- **水たまり `puddle_reflection`** は Act IV 以降の湿り気表現として、2-4 箇所に偶発的に置く（焦点セル依存ではない）。
- **見分けテスト**: パッチの切れ目で「どのセルが焦点なのか」が建物を見なくても読めること。

これにより、「Act 基調 → 焦点パッチ → 焦点建物」の 3 層で読ませる。パッチだらけだと基調が死に、焦点が目立たなくなる。

---

# §6 横断要件

## 座標系

- dx: ワールド X ∈ [-180, +180]
- dy: チャンクローカル ∈ [0, 200]
- チャンク境界は y=0 / y=200
- facade 線は y=22（上段）と y=118（下段）
- 中央 avenue 帯は y=85-115

## ヘルパー（stages.ts 冒頭で定義済み）

- `_B(size, dx, dy)` — 建物
- `_F(type, dx, dy)` — 家具
- `_H(dx, dy)` — 人
- `_G(type, dx, dy, w, h)` — 地面
- `_HR(dy, xMin, xMax, cls?)` — 水平道路（部分幅 / 全幅）
- `_VR(dx, yMinLocal, yMaxLocal, cls?)` — 垂直道路（部分高さ / 全高）
- `_AVE` — **Stage 1 の唯一の固定縦軸**。中央 avenue（x=0、w=18）。全チャンクで `verticalRoads` の先頭に必ず置く
- `_SPINE_V` — 後方互換用の縦 3 本（x=-90 / 0 / +90）。Stage 1 の新規 raw では原則使わない
- `_MID_HR` — 中央クロス街路のプリセット（y=100、全幅）。**便利だが必須ではない**。チャンクのコンセプトが y=100 全幅の横道路を求める場合のみ採用
- `_TOP_HR` — 上端クロス街路のプリセット（y=200、全幅）。Act 境界・接続点が必要な場合のみ採用
- `horizontalRoads` — raw チャンク内の水平道路配列。**空配列 `[]` も許容**（横道路ゼロのチャンクが成立する）
- `verticalRoads` — raw チャンク内の縦道路配列。**先頭に `_AVE` 必須**、追加 `_VR` はチャンクごとに採否を決める

## 既存 union の厳守

詳細フェーズで使える型は以下のみ。新規追加は別タスク扱い。

- **FurnitureType**（約 100 種、`entities.ts`）
- **BuildingSize**（約 80 種、`constants.ts`）
- **GroundType**（18 種、`scenes.ts`）

## チャンク境界の連続性

- y=0 / y=200 近傍に対で要素を配置し、隣接チャンクと視覚的に繋げる
- 並木の繋ぎ、歩道の切れ目を作らない
- 電柱は左右端 y=92 / y=195 を対で配置（既存パターン踏襲）

## 精巧さの最低ライン

- 各チャンクの Act ごとの **密度下限目安** を下回らない（**建物数・家具数・人数はいずれも下限**として扱う。上限は設けず、可読性・処理負荷・当たり判定が破綻しない範囲で超過可）
- 各チャンクは **ヒーロー 1-2 セル + アンビエント 2-3 セル** の構成（§3.6）。マージチャンク（Ch5 / Ch6 / Ch7）は「1 大ヒーロー」扱いで例外（アンビエントセルなしで OK）
- 各チャンクに **1 つ以上** 公園モデル 4 層を満たす完成エリアがあること
- 各セルに **生活の痕跡要素 1 個以上**（§4.8: laundry / 傾いた bicycle / fallen_leaves / half-open noren / mailbox flag / trash_bag 等）
- 各セル内で **スケール階層が成立**（§4.8: 大 1 : 中 2-3 : 小 10-20、同サイズ連続禁止）
- 全チャンクで **1 匹以上** の `cat`（象徴的生活感）
- **孤立電柱禁止**: `power_pole` を置いたら `power_line` を 2 本以上接続、または近接電柱と対で配置
- **向きの一貫性**: ベンチ向き / 旗向き / 自販機向き 等が §4.8 の向きルールに沿っていること

## チャンク境界 handoff 記述フォーマット

詳細フェーズで各チャンクの末尾に、**次チャンク冒頭との繋ぎ**を明示する。1 チャンクにつき以下の 4 項目を埋める。

- **並木**: 自チャンク y=180-200 と次チャンク y=0-20 で同じ樹種（sakura_tree / tree / pine_tree）の並木が切れずに連続していること。樹種が切り替わる Ch（Ch5→Ch6、Ch8→Ch9）では**切替地点の x 座標**を明記
- **歩道 / 道路**: x=-65 の stone_pavement 帯（または Act 基調の歩道タイプ）が y=200 ↔ y=0 で連続していること。加えて、Stage 1 では中央 `_AVE`（x=0）が全チャンクで途切れないこと。`_TOP_HR` や部分幅道路は、隣接チャンクと接続する場合だけ handoff に明記する
- **電柱/電線**: y=92 / y=195 の対で、次チャンク冒頭の電柱と電線が繋がること
- **Act 切替の予兆**: Act 末尾チャンク（Ch2 / Ch5 / Ch8 / Ch11）では次 Act の象徴要素を 1-2 個だけ先行配置（例: Act III→IV の Ch8 下段に `pine_tree`）

この 4 項目が埋まらないチャンクは、詳細フェーズの review で戻す。

## セル境界の連続性

チャンク内の **セル境界**（x=0 avenue 両脇、y=100 MID_HR 両側）でも、視覚的不連続を作らない。

- **facade 線の連続**: 上段の NW/NE は facade 線 y=22 で同じ高さに揃える。下段 SW/SE も y=118 で揃える。avenue (x=0) を跨いでも段差ができないこと
- **歩道の連続**: x=-65 の `stone_pavement` 帯は y=0〜200 で切れない。NW→SW の接続で途切れないこと
- **中央 `_AVE` の連続**: x=0 avenue は Stage 1 の基本縦軸として全チャンクで維持する。x=-90 / +90 の旧 spine は固定前提にしない
- **avenue 手前の切れ目禁止**: avenue を跨ぐ歩行者歩道帯（y=92 / y=108）は x=-10〜+10 で切らない
- **焦点の両側バッファ**: 焦点建物は avenue / MID_HR / 追加 `_HR` / 追加 `_VR` から **5 px 以上** 離す（建物が道路に食い込むと不自然）
- **境界フェンス/生垣の通し**: `wood_fence` / `hedge` の列は、セル境界でプツッと終わらせず、avenue 手前で自然にフェードアウト（間隔を徐々に広げる）
- **街路樹の縦方向連続**: 街路樹列（sakura_tree / tree / pine_tree）は NW-SW / NE-SE の縦方向（セルを跨ぐ）でも等間隔を保つ

セルは論理的な区分けであり、視覚的には「なめらかに続く街」として読めること。

---

# §6.5 実 union マッピング表（実装前必読）

<aside>
🚨

**ドキュメント内の型名が実コードの union と一致しない箇所がある**。§9 の TS スケルトンをそのまま貼るとコンパイルエラーになるので、下記の対応表で置換してから適用する。型の確定ソース: `src/constants.ts`（`BuildingSize`）/ `src/entities.ts`（`FurnitureType`）/ `src/scenes.ts`（`GroundType`）。

</aside>

## 建物 (BuildingSize) の置換表

| ドキュメント中の名前 | 実 BuildingSize | 推奨置換 | 備考 |
| --- | --- | --- | --- |
| `residential_wood` | なし | `kominka`（木造和風） or `bungalow`（木造小住宅） | Act I 住宅街には `kominka` が質感合う |
| `residential_modern` | なし | `townhouse` / `mansion` / `house` | Act I は `townhouse`、Act II 都心寄りは `mansion` |
| `cottage_2f` | なし | `duplex` or `house` | 2階建ての小住宅 |
| `bathhouse` | なし | `onsen_inn` | 銭湯の完全一致はなく `onsen_inn` で近似。識別性は `bathhouse_chimney`  • `noren`  • `milk_crate_stack` の取り巻きで担保 |

## 家具 (FurnitureType) の置換表

| ドキュメント中の名前 | 実 FurnitureType | 推奨置換 | 備考 |
| --- | --- | --- | --- |
| `laundry_on_wire` | なし | `laundry_pole` or `laundry_balcony` | 2F住宅・現代住宅は `laundry_balcony`、1F縁側・庭は `laundry_pole` |
| `trash_bag` | なし | `garbage` | ゴミ出し痕跡 |
| `vending_machine` | なし | `vending` | 命名の省略形。全箇所で `vending` に統一 |
| `fallen_leaves` | **FurnitureType にはない**（`GroundType` として存在） | `_F` → `_G('fallen_leaves', x, y, w, h)` に変更（w, h は 20〜40 の小パッチ） | 落ち葉は地面タイプ。`_F('fallen_leaves', ...)` は型エラー |

## 型の取り違え (Furniture vs Ground)

- `fallen_leaves` は **GroundType**。§9 の全箇所で `_F('fallen_leaves', x, y)` を `_G('fallen_leaves', x, y, w, h)` に置換する。
- その他のテクスチャ系（`dirt` / `grass` / `concrete` / `wood_deck` / `residential_tile` / `stone_pavement` / `tile` / `asphalt`）は既に `_G` で記述済み。`_F` 側には混入させないこと。

## 「生活の痕跡」再定義（§4.8 との差分）

§4.8 で挙げた痕跡候補のうち、実際に配置可能なもの:

- ✅ 配置可: `laundry_pole` / `laundry_balcony`（物干し）、`garbage`（ゴミ）、`potted_plant` / `flower_planter_row`（鉢の不揃い）、`bicycle_row`（傾けて置く）、`cat`、`mailbox`
- ❌ 代替必須: `laundry_on_wire` → `laundry_pole` or `laundry_balcony`、`trash_bag` → `garbage`、`bottle_recycle` → `recycling_bin`
- ⚠️ 代用なし（コメント記述のみ）: 半開きの `noren` の揺れ、`banner` の風、`mailbox` の flag_up — アニメーション/状態管理が未実装のため

## 実 union サマリ（2026-04-25 時点、GitHub main ブランチより抽出）

- **BuildingSize** (`src/constants.ts`): house / shop / apartment / office / tower / skyscraper / convenience / restaurant / school / hospital / temple / parking / townhouse / mansion / garage / shed / greenhouse / daycare / clinic / shrine / apartment_tall / cafe / bakery / bookstore / pharmacy / supermarket / karaoke / pachinko / laundromat / florist / ramen / izakaya / game_center / bank / post_office / library / museum / city_hall / fire_station / police_station / train_station / movie_theater / gas_station / clock_tower / radio_tower / ferris_wheel / stadium / water_tower / department_store / warehouse / crane_gantry / container_stack / factory_stack / silo / pagoda / ryokan / kominka / chaya / carousel / roller_coaster / yatai / big_tent / shotengai_arcade / bus_terminal_shelter / fountain_pavilion / snack / love_hotel / business_hotel / mahjong_parlor / club / capsule_hotel / kura / machiya / onsen_inn / tahoto / dojo / wagashi / kimono_shop / sushi_ya / bungalow / duplex / castle
- **GroundType** (`src/scenes.ts`): concrete / stone_pavement / asphalt / wood_deck / tile / residential_tile / grass / dirt / fallen_leaves / gravel / steel_plate / oil_stained_concrete / harbor_water / rust_deck / hazard_stripe / moss / red_carpet / checker_tile
- **FurnitureType** (`src/entities.ts`): tree / vending / bench / car / traffic_light / mailbox / bicycle / flower_bed / parasol / sign_board / garbage / power_pole / hydrant / fountain / street_lamp / bollard / traffic_cone / barrier / guardrail / telephone_booth / electric_box / newspaper_stand / atm / post_box / bicycle_rack / dumpster / recycling_bin / fire_extinguisher / bus_stop / statue / flag_pole / banner_pole / bush / hedge / planter / sakura_tree / pine_tree / palm_tree / bamboo_cluster / torii / stone_lantern / shinto_rope / offering_box / chouchin / noren / a_frame_sign / shop_awning / milk_crate_stack / wood_fence / laundry_pole / ac_unit / gas_canister / potted_plant / rock / stepping_stones / koi_pond / bonsai / street_mirror / tarp / sandbags / water_tank / drum_can / cargo_container / forklift / buoy / pallet_stack / koma_inu / ema_rack / bamboo_fence / temizuya / balloon_cluster / ticket_booth / matsuri_drum / popcorn_cart / cat / ac_outdoor_cluster / power_line / laundry_balcony / kerbside_vending_pair / post_letter_box / flower_planter_row / guardrail_short / railway_track / platform_edge / railroad_crossing / pedestrian_bridge / signal_tower / plaza_tile_circle / fountain_large / taxi_rank_sign / sando_stone_pillar / ema_wall / omikuji_stand / shrine_fence_red / bamboo_water_fountain / puddle_reflection / manhole_cover / cable_junction_box / bicycle_row / play_structure / slide / swing_set / sandbox / bathhouse_chimney / jungle_gym / fire_watchtower / grain_silo

## 実装手順

1. §9 の TS スケルトンを **参考** として読む（道路部分は例示扱い、コンセプトに沿って再設計してよい）
2. §5 の **チャンク道路の設計手順** に従い、コンセプト → 動線 → 焦点境界 → 道路セグメントの順で道路を決める
3. `verticalRoads` の先頭に `_AVE` が入っていることを確認する。`_SPINE_V` を使っていないことを確認する
4. `horizontalRoads` の各エントリについて「なぜこの道があるか」を一言で説明できることを確認する（説明できない道は削る）
5. 全 `_F` / `_B` / `_G` の第一引数が実 union 名であることを確認する
6. `fallen_leaves` が `_G('fallen_leaves', x, y, w, h)` として置かれていることを確認する
7. `tsc --noEmit` で union 型チェック。残るエラーは §6.5 の置換表に追加してから再対応

---

# §6.6 SemanticCluster — 配置に視覚的意味を持たせるレイヤー (v6.3+)

<aside>
🎯

**目的**: 配置を「フラットなオブジェクトの羅列」から **「焦点・取り巻き・境界・動線・痕跡で構成された意味のある場面」** に格上げする。指示書 §3.6 / §4 / §6 の原則を **データ構造とリンタで強制可能** にする。

</aside>

## データ構造（実装済み: `src/stages.ts`）

`RawChunkBody.clusters?: SemanticCluster[]` （オプショナル、後方互換 100%）。

```ts
interface SemanticCluster {
  id: string;                                         // 'ch1.SE.park' 等、Lint レポート用
  role: 'hero' | 'ambient';                           // 焦点 or 取り巻き役
  cell: 'NW' | 'NE' | 'SW' | 'SE' | 'merged';         // §3.5 セル帰属
  focal: { kind: 'b'|'f', i: number };                // 焦点 1 個
  companions?: Ref[];                                 // 取り巻き (hero は 3-5 個目安)
  boundary?: Ref[];                                   // 境界 (hedge / wood_fence)
  access?: Ref[];                                     // 動線 (lamp / stepping_stones)
  livingTrace?: Ref;                                  // 生活痕跡 (laundry / garbage)
  handoffTo?: 'next' | 'prev';                        // 連続軸マーカー
}
```

**Ref 形式**: `{ kind: 'b' | 'f', i: number }` で `buildings[i]` または `furniture[i]` を指す。配列途中への挿入禁止（末尾追加なら Ref が安定）。

## ヘルパー DSL

```ts
const $B = (out, size, dx, dy): BRef    // buildings に push しつつ Ref 返却
const $F = (out, type, dx, dy): FRef    // furniture に push しつつ Ref 返却
const _ROW = (out, type, dx, dys[])     // 同 dx 複数 dy の家具列を一発作成
const _CLUSTER = (out, cluster)         // out.clusters?? に push
```

`$B/$F` は既存 `_B/_F` と互換。クラスタを書きたいときだけ使う。それ以外の家具は従来の `_B/_F` で良い。`_HOUSEHOLD` / `_PARK` のような型別ラッパーは作らない（過剰抽象化を避ける）。

## 自動リンタ (`npm run lint:placement`)

| ID | ルール | レベル | 出典 |
|---|---|---|---|
| L0 | Ref 解決チェック (index 範囲) | error | 整合性 |
| L1 | 各クラスタに focal が 1 つ | error | §4 |
| L2 | hero クラスタの companions 数 3-5 | warn | §4 焦点囲み |
| L3 | 各セルに livingTrace 1 個以上 | warn | §6 |
| L4 | 4+ 同種家具の同 dy 等間隔（行進） | warn | §6 |
| L5 | 同種家具の鏡像対称ペア（\|a+b\|<3）| warn | §6（"2-5px ずらす"） |
| L6 | 同サイズ建物の dx 隣接（差<40） | warn | §6 |
| L7 | 孤立 power_pole（power_line 不足） | warn | §6 |
| L8 | hero クラスタが avenue / y=100 で隣接 | warn | §3.6 |
| L9 | 看板系家具が facade 線（y=22±4）から逸脱 | info | §4 |

連続軸（桜並木・街灯列）は SPINE_TYPES として L4/L5 から自動除外。

## 環境パーティクル (実装済み: `particles.spawnAmbient`)

各 hero クラスタの focal 種別から ambient 種を派生し、低レート（0.5 秒間隔）で常時 emit:

| focal 種別 | ambient | 視覚効果 |
|---|---|---|
| `kominka` / `onsen_inn` / `train_station` | steam | 蒸気立ち上り |
| `bathhouse_chimney` | steam | 煙突の煙 |
| `sakura_tree` | sakura | 桜吹雪 |
| `koi_pond` / `fountain` / `fountain_large` | water | 水しぶき |
| `play_structure` / `sandbox` / `jungle_gym` | dust | 子供が遊ぶ砂塵 |
| `statue` / `plaza_tile_circle` | firefly | 黄緑の光 (広場) |
| `gas_station` / `school` / `warehouse` | dust | 砂塵 |
| `grain_silo` | dust | 農塵 |

画面外チャンクは emit 停止。最大 12 チャンク × 1-2 hero / chunk = 24 emitter で `MAX_PARTICLES = 2000` に対し負荷軽微。

## 実装手順（新規チャンク or リファクタ時）

1. 通常通り `_B / _F / _G / _H` で配置を書く（既存パターン）
2. `clusters: [...]` を `raw` の末尾に追加し、§3.6 のジグザグ流れに沿った hero 1 個 + ambient 0-3 個を宣言
3. focal は **オープンスペース焦点**（公園・広場・駅前）なら家具 (`{ kind: 'f', i: N }`)、**焦点建物**（kominka・銭湯・学校）なら建物 (`{ kind: 'b', i: N }`)
4. companions は 3-5 個（焦点を「囲む」家具・建物を選ぶ）
5. livingTrace を **各セル 1 個** 必ず指定（laundry / garbage / bicycle / cat / mailbox / potted_plant のいずれか）
6. `npm run lint:placement` で確認、warn を最小限に減らす
7. 既存の `_B/_F` を `$B/$F` に置換すれば名前付き Ref が使える（IIFE wrap が必要）

## 設計原則の対応表

| 指示書原則 | SemanticCluster での実装 | リンタルール |
|---|---|---|
| §3.6 ヒーロー/アンビエント階層 | `role: 'hero'` / `'ambient'` | L8 隣接禁止 |
| §4 公園モデル 4 層 | `focal` / `companions` / `boundary` / `access` | L1 焦点必須、L2 取り巻き 3-5 |
| §4.5 建物→取り巻き辞書 | `focal` 建物 + `companions` 家具で表現 | （文書原則） |
| §4.7 取り巻き 3 パターン | `companions` (焦点囲み) / `boundary` / `access` | L2 |
| §4.8 スケール階層 1:3:20 | （現状未強制、将来 L11 候補） | — |
| §4.8 向きの意図 | （現状 facing 未実装、§4.8 既知 TODO） | — |
| §6 行進禁止 | — | L4 |
| §6 完全対称禁止 | — | L5 |
| §6 同サイズ連続禁止 | — | L6 |
| §6 孤立電柱禁止 | — | L7 |
| §6 生活の痕跡 各セル 1 個以上 | `livingTrace` | L3 |
| §6 facade 線整列 | — | L9 |

---

# §7 次フェーズで詳細化する項目

この方針が承認されたら、以下を **チャンクごとに** 書き下ろす:

- [ ]  サブエリア（セル A-H）の焦点建物と公園モデル 4 層の内訳
- [ ]  追加/変更する道路（部分幅水平道路、細い小道）
- [ ]  grounds パッチのレイアウト（Act 基調 + 焦点エリアの強調色）
- [ ]  取り巻き家具の具体座標（対称性のズラし含む）
- [ ]  人配置（Act 人流グラデに沿って）
- [ ]  チャンク境界の handoff 要素
- [ ]  最終的な raw ChunkTemplate の TS リテラル

---

# §9 詳細フェーズ: 各チャンクレイアウト

<aside>
📐

**読み方**: 各チャンクは以下 7 項目で構成（§7 checklist を実装）。(1) ヒーロー/アンビエント割当（§3.6）/ (2) ヒーローセルの焦点 4 層（§4 4層構造）/ (3) 取り巻き 3 パターン分類（§4.7）/ (4) 人配置（§4.6）/ (5) 地面パッチ・追加道路（§5）/ (6) 生活の痕跡・スケール階層・向き（§4.8）/ (7) Handoff 4 項目（§6）。末尾に raw ChunkTemplate の TypeScript スケルトン。座標は全て dx ∈ [-180, +180] / dy ∈ [0, 200] のチャンクローカル。TS では見やすさ優先で single-quote (`'`) を使用（プロジェクトの quote 設定に合わせて置換してよい）。型名は §6.5 の実 union 名で統一する。

**🛣️ 道路に関する注意（v6.2）**: 以下各チャンクの **道路記述・スケルトン中の `horizontalRoads` / `verticalRoads` は v6.1 当時の例示**。実装時は §5 の「チャンク道路の設計手順」に従い、チャンクのコンセプト・動線・焦点境界から道路を **再設計してよい**。固定するのは `_AVE`（x=0 縦軸）のみ。`_MID_HR` を含む横道路と、`_AVE` 以外の縦道路はすべてチャンクごとに採否を判断する。「なんとなく `_MID_HR` を置く」「他のチャンクと揃える」は理由として不可。

</aside>

<aside>
🌊

**Stage 全体ジグザグ流れ（決定版）**

Ch0 **NW** → Ch1 **SE** → Ch2 **NW+NE マージ** → Ch3 **SW+SE マージ** → Ch4 **NW** → Ch5 **SW+SE マージ** → Ch6 **全マージ** → Ch7 **NW+NE マージ** → Ch8 **NE** → Ch9 **SE** → Ch10 **NW+NE マージ** → Ch11 **SW+SE マージ**。

クライマックス = Ch5/Ch6/Ch7 の 3 連続大ヒーロー（Act II→III 境界例外）。Ch8 の single-cell ヒーローで呼吸を入れ、Ch9 以降で Act IV へ。

</aside>

## Act I — 閑静な住宅街

### Ch0: 日本庭園と縁側の家

**ヒーロー / アンビエント**: **NW = ヒーロー**（日本庭園 オープンスペース A）/ NE・SW・SE = アンビエント（住宅）

**NW ヒーロー 4 層**:

- **焦点**: `koi_pond` (x=-90, y=45) + `stepping_stones` × 3（弧 x=-70/-90/-110, y=55-65）+ `bonsai` × 3（弧 x=-120/-90/-60, y=35-40）
- **取り巻き（焦点囲み）**: `pine_tree` × 2 (x=-150/-50, y=50)、`stone_lantern` (x=-110, y=58)、`potted_plant` × 3 縁側前
- **境界**: `wood_fence` 北端 y=5 (x=-100/-50)、`hedge` 南縁 (x=-30, y=95)、`wood_fence` 西端 (x=-178)
- **動線**: 縁側 `wood_deck` パッチ (x=-60, y=85, 70×20)、`stepping_stones` が東から庭中央へ誘導

**アンビエント 3 セル**:

- **NE**: `house` (x=100, y=20) + 傾き `bicycle` (x=120, y=45) + `potted_plant` × 3
- **SW**: `kominka` (x=-110, y=120) + `laundry_pole` (x=-140, y=160) + `cat` (x=-160, y=170)
- **SE**: `house` (x=90, y=120) + `garbage` (x=160, y=185) + `potted_plant` × 3

**取り巻き 3 パターン**:

- **facade 沿い** (y=22): `mailbox` × 4 不均等 (x=-155 / -50 / +30 / +150)
- **焦点囲み**: NW 庭園に集約（上記）
- **歩道沿い** (x=-65 帯): `street_lamp` × 2 (y=60/160)、`sakura_tree` 西列 × 3 (x=-170, y=25/95/190)、東列 × 2 (x=170, y=30/130)

**人配置**: 老人 × 2 縁側 (x=-55/-45, y=82-85、koi_pond 向き)、園児+保護者 NE 住宅前 (x=55, y=130)、`cat` × 2（縁側脇 x=-75 y=88、SW 塀際 x=-160 y=170）

**地面パッチ**:

- 基調: `residential_tile` 全面
- NW 焦点: `grass` 大パッチ (center x=-90 y=45, 170×95)、`dirt` koi_pond 下 (x=-90 y=43, 35×20)、`wood_deck` 縁側 (x=-60 y=85, 70×20)
- 歩道帯: `stone_pavement` (x=-65 y=100, 12×200)

**道路**: `verticalRoads: [_AVE]` / `horizontalRoads: [_MID_HR]`（追加なし）

**生活の痕跡 / スケール / 向き**:

- **痕跡**: `fallen_leaves` ground 小パッチ NW (x=-130 y=75)、傾き `bicycle` NE、`laundry_pole` SW、`garbage` SE — 全 4 セル 1 個以上 ✅
- **スケール**: 大 `koi_pond` / 中 `pine_tree` × 2・`stone_lantern` / 小 `bonsai` × 3・`mailbox` × 4・`potted_plant` × 12+ — 1:3:20 比
- **向き**: 縁側の老人は庭（南向き）、`mailbox` flag 北向き（朝刊配達直後）、`bicycle` は facade に傾き、`cat` は庭方向、`stone_lantern` は縁側側に開口

**Handoff (Ch0→Ch1)**:

- **並木**: `sakura_tree` (x=-170, y=190) ↔ Ch1 y=10 に `sakura_tree`、樹種・x 座標 一致
- **歩道**: `stone_pavement` (x=-65) 帯継続
- **電柱/電線**: `power_pole` (x=-178/+178, y=195) 対 + `power_line` y=195 → Ch1 y=5 に対
- **予兆**: なし（Act I 内継続）

**TypeScript スケルトン**:

```tsx
const CH0: ChunkTemplate = {
  title: '日本庭園と縁側の家',
  buildings: [
    _B('kominka', -80, 78),               // NW 主屋（縁側側）
    _B('house', 100, 20),                 // NE 住宅
    _B('kominka', -110, 120),             // SW 住宅
    _B('house', 90, 120),                 // SE 住宅
  ],
  furnitures: [
    // ── NW 焦点: 日本庭園 ──
    _F('koi_pond', -90, 45),
    _F('stepping_stones', -70, 55), _F('stepping_stones', -90, 65), _F('stepping_stones', -110, 55),
    _F('bonsai', -60, 38), _F('bonsai', -90, 30), _F('bonsai', -120, 38),
    _F('pine_tree', -150, 50), _F('pine_tree', -50, 50),
    _F('stone_lantern', -110, 58),
    _F('cat', -75, 88),
    _F('wood_fence', -100, 5), _F('wood_fence', -50, 5),
    _F('hedge', -30, 95),
    _F('potted_plant', -40, 80), _F('potted_plant', -70, 92), _F('potted_plant', -90, 92),
    // ── NE アンビエント ──
    _F('bicycle', 120, 45),
    _F('potted_plant', 50, 35), _F('potted_plant', 80, 35), _F('potted_plant', 140, 35),
    // ── SW アンビエント ──
    _F('laundry_pole', -140, 160),
    _F('cat', -160, 170),
    _F('potted_plant', -80, 135), _F('potted_plant', -130, 135),
    _F('wood_fence', -50, 195),
    // ── SE アンビエント ──
    _F('garbage', 160, 185),
    _F('potted_plant', 50, 135), _F('potted_plant', 120, 135), _F('potted_plant', 160, 135),
    // ── facade 沿い mailbox 列（不均等 4 個）──
    _F('mailbox', -155, 22), _F('mailbox', -50, 22), _F('mailbox', 30, 22), _F('mailbox', 150, 22),
    // ── 歩道沿い ──
    _F('street_lamp', -70, 60), _F('street_lamp', -70, 160),
    _F('sakura_tree', -170, 25), _F('sakura_tree', -170, 95), _F('sakura_tree', -170, 190),
    _F('sakura_tree', 170, 30), _F('sakura_tree', 170, 130),
    _F('power_pole', -178, 195), _F('power_pole', 178, 195), _F('power_line', -90, 195),
  ],
  humans: [_H(-55, 82), _H(-45, 85), _H(55, 130)],
  grounds: [
    _G('residential_tile', 0, 100, 360, 200),
    _G('grass', -90, 45, 170, 95),
    _G('dirt', -90, 43, 35, 20),
    _G('stone_pavement', -65, 100, 12, 200),
    _G('wood_deck', -60, 85, 70, 20),
    _G('fallen_leaves', -130, 75, 30, 20),
  ],
  horizontalRoads: [_MID_HR],
  verticalRoads: [_AVE],
};
```

### Ch1: 児童公園と周辺住宅 ⭐ 見本チャンク（v6.1 整合性反映済み）

**ヒーロー / アンビエント**: **SE = ヒーロー**（児童公園 オープンスペース A）/ NW・NE・SW = アンビエント（住宅）

**SE ヒーロー 4 層**:

- **焦点**: `play_structure` (x=90, y=145) + `swing_set` (x=50, y=140) + `slide` (x=130, y=140) + `sandbox` (x=90, y=175)
- **取り巻き（焦点囲み）**: `bench` × 3 準対称 (x=30/90/150, y=122-125、すべて遊具中央向き)、`flower_bed` × 2 (x=60/120, y=120)、`street_lamp` × 2 (x=30/160, y=132)
- **境界**: `hedge` 公園南縁 y=195 (x=30/90/150)、`flower_planter_row` 北縁 y=108 (x=20/60/100/140)
- **動線**: 公園入口 (x=5, y=108) avenue 接続、`tile` 公園敷石パッチ

**アンビエント 3 セル**:

- **NW**: `townhouse` (x=-100, y=20) + 傾き `bicycle_row` (x=-140, y=40) + `potted_plant` × 3
- **NE**: `kominka` (x=80, y=20) + `mailbox` (x=120, y=22) + `cat` (x=170, y=35)
- **SW**: `house` (x=-100, y=120) + `laundry_balcony` (x=-140, y=160) + `wood_fence` 南縁

**取り巻き 3 パターン**:

- **facade 沿い** (y=22): `mailbox` × 3 (x=-130 / 20 / 150) — SE は公園入口避け省略
- **焦点囲み**: SE 公園遊具周囲（上記）
- **歩道沿い** (x=-65): `street_lamp` × 2 (y=60/160)、`sakura_tree` 西列 × 3 (x=-170, y=30/100/190)、東列 × 2 (x=170, y=30/90)

**人配置**: 園児 × 3（sandbox 内 / swing 上 / slide 上）、保護者 × 2 ベンチ (x=30/150, y=125 子供方向)、園児+保護者 歩道 (x=-65, y=60-70)、`cat` × 1

**地面パッチ**:

- 基調: `residential_tile`
- SE 焦点: `tile` 公園敷石 (x=90 y=150, 170×90)
- 歩道帯: `stone_pavement` (x=-65 y=100, 12×200)
- **道路**: `verticalRoads: [_AVE]` / `horizontalRoads: [_MID_HR, _HR(130, 10, 180)]`（公園前アクセス道）

**生活の痕跡 / スケール / 向き**:

- **痕跡**: `fallen_leaves` ground 小パッチ 公園隅 (x=170, y=180)、傾き `bicycle_row` NW、`laundry_balcony` SW、`potted_plant` 微揺れ NE
- **スケール**: 大 `play_structure` / 中 `slide`・`swing_set` / 小 `bench` × 3・`flower_bed`・`potted_plant` × 10+
- **向き**: ベンチ 3 つ全て公園中央向き、保護者→子供、`cat` は公園側、園児たちは遊具方向

**Handoff (Ch1→Ch2)**:

- **並木**: `sakura_tree` (x=-170, y=190) ↔ Ch2 y=10 一致
- **歩道**: `stone_pavement` 連続
- **電柱/電線**: `power_pole` (x=178, y=195) + `power_line` → Ch2
- **予兆**: Ch2 の `_TOP_HR` 予告として、y=195 付近の `hedge` を avenue (x=-10〜+10) 寄りに集約して Act I 末の締まりを作る

**TypeScript スケルトン**:

```tsx
const CH1: ChunkTemplate = {
  title: '児童公園と周辺住宅',
  // v6.1 canonical 見本: SE ヒーロー（公園） / NW・NE・SW アンビエント（住宅）
  // ジグザグ流れ (§3.6): Ch0 NW → Ch1 SE → Ch2 NW+NE マージ
  buildings: [
    // NW アンビエント: 現代住宅、facade y=22（上段）
    _B('townhouse', -100, 20),        // 元 residential_modern (§6.5 置換)
    // NE アンビエント: 木造住宅、facade y=22
    _B('kominka', 80, 20),            // 元 residential_wood (§6.5 置換)。Act I 基調を強化
    // SW アンビエント: シンプル2階建て、facade y=118（下段）
    _B('house', -100, 120),           // 元 cottage_2f (§6.5 置換)
    // SE = 公園ヒーロー。無建物（オープンスペース焦点 = 基本形 A）
  ],
  furnitures: [
    // ── SE 焦点: 児童公園（基本形 A）──
    // 遊具 4 種を中央集約。識別性テスト: これだけで「公園」と読める
    _F('play_structure', 90, 145),    // 焦点コア: 複合遊具（最大）
    _F('swing_set', 50, 140),         // 焦点左: play_structure の -40 px、対称軸
    _F('slide', 130, 140),            // 焦点右: play_structure の +40 px、対称軸
    _F('sandbox', 90, 175),           // 焦点下: y=175（南に 30 px オフセット）
    // 取り巻き（焦点囲み型 §4.7）: ベンチ 3 つ。遊具手前 → 保護者席として読める配置
    _F('bench', 30, 125),             // 左ベンチ（遊具方向を向く意図）
    _F('bench', 90, 122),             // 中央ベンチ（y=122、左右より 3 px 上、左右非対称 §4.6）
    _F('bench', 150, 125),            // 右ベンチ
    // 花壇（取り巻き、遊具両脇）
    _F('flower_bed', 60, 120),        // swing_set 前
    _F('flower_bed', 120, 120),       // slide 前
    // 街灯（取り巻き、公園四隅に近い位置）
    _F('street_lamp', 30, 132),       // 左
    _F('street_lamp', 160, 132),      // 右（x=160 で完全対称を崩す）
    // 境界 §4.4: プランター列で北縁、hedge で南縁
    _F('flower_planter_row', 20, 108),   // 北縁、MID_HR y=100 の直下 8 px
    _F('flower_planter_row', 60, 108),
    _F('flower_planter_row', 100, 108),
    _F('flower_planter_row', 140, 108),  // 4 個等間隔で公園の北縁を示す
    _F('hedge', 30, 195),             // 南縁、チャンク下端 y=200 の直上 5 px
    _F('hedge', 90, 195),
    _F('hedge', 150, 195),            // 3 個（奇数、§4.6 非対称）
    // ── NW アンビエント（現代住宅）──
    _F('bicycle_row', -140, 40),      // 傾けた駐輪（生活の痕跡 §4.8）
    _F('potted_plant', -60, 35),      // facade 前の鉢
    _F('potted_plant', -130, 35),
    _F('potted_plant', -150, 40),     // y を +5 ずらして「置き主の性格」
    // ── NE アンビエント（木造住宅）──
    _F('mailbox', 120, 22),           // facade 線 y=22 上の郵便受け
    _F('cat', 170, 35),               // NE 隅の野良猫（象徴的生活感）
    _F('potted_plant', 50, 35),
    _F('potted_plant', 150, 35),
    // ── SW アンビエント（2 階建て住宅）──
    _F('laundry_balcony', -140, 160), // 元 laundry_on_wire (§6.5 置換)。2F ベランダ物干し
    _F('wood_fence', -50, 195),       // 南縁フェンス（アンビエント境界）
    _F('potted_plant', -80, 135),
    _F('potted_plant', -130, 135),
    // ── facade 沿い共通 mailbox（不均等 §4.6）──
    _F('mailbox', -130, 22),          // NW 住宅の郵便受け
    _F('mailbox', 20, 22),            // avenue 脇、公園入口を避けて x=+20
    // ── 歩道沿い（x=-65 歩道帯 + x=±170 並木）──
    _F('street_lamp', -70, 60),       // 歩道帯 上半
    _F('street_lamp', -70, 160),      // 歩道帯 下半
    _F('sakura_tree', -170, 30),      // 西並木 上
    _F('sakura_tree', -170, 100),     // 西並木 中（MID_HR レベル）
    _F('sakura_tree', -170, 190),     // 西並木 下（Ch2 handoff 点、§6 並木連続）
    _F('sakura_tree', 170, 30),       // 東並木 上
    _F('sakura_tree', 170, 90),       // 東並木 中（西 y=100 と 10 px ずらして非対称）
    // 電柱+電線（孤立禁止 §6）
    _F('power_pole', -178, 195),
    _F('power_pole', 178, 195),
    _F('power_line', -90, 195),       // 両電柱間を繋ぐ
  ],
  humans: [
    // 園児 × 3（遊具と同座標 = その遊具で遊んでいる）
    _H(90, 175),                      // 砂場の子
    _H(50, 140),                      // ブランコの子
    _H(130, 140),                     // すべり台の子
    // 保護者 × 2（ベンチと同座標 = 子を向いて座る意図）
    _H(30, 125),                      // 左ベンチ保護者
    _H(150, 125),                     // 右ベンチ保護者
    // 歩道の園児+保護者（§4.6 歩行者）
    _H(-65, 60),
    _H(-65, 70),
  ],
  grounds: [
    // Act I 基調: residential_tile 全面 (§5)
    _G('residential_tile', 0, 100, 360, 200),
    // SE 焦点パッチ: 公園敷石 tile (170×90)、焦点+取り巻きを包む範囲（§5 焦点矩形 1.2 倍以内）
    _G('tile', 90, 150, 170, 90),
    // 歩道帯: stone_pavement x=-65 連続（§3 連続軸、Ch0 → Ch2 へ継続）
    _G('stone_pavement', -65, 100, 12, 200),
    // 生活の痕跡: 公園隅の落ち葉パッチ（元 _F('fallen_leaves') § 6.5 型取り違え修正）
    _G('fallen_leaves', 170, 180, 25, 20),
  ],
  horizontalRoads: [_MID_HR, _HR(130, 10, 180)],
  verticalRoads: [_AVE],
};
```

### Ch2: 公共 3 連 + 歩道橋 + _TOP_HR（Act I 末）

**ヒーロー / アンビエント**: **NW+NE マージ = 大ヒーロー**（保育園+診療所+郵便局 クラスター C）/ SW・SE = アンビエント

**NW+NE 大ヒーロー 4 層**:

- **焦点 (3 棟 facade y=22)**: `daycare` (x=-110, y=22) + `clinic` (x=0, y=22) + `post_office` (x=110, y=22) + `pedestrian_bridge` (x=0, y=60) avenue 跨ぎ
- **取り巻き per 建物**:
    - **daycare 園庭** (x=-160〜-60, y=50-95): `swing_set` (x=-140, y=70) + `slide` (x=-80, y=80) + `sandbox` (x=-120, y=85) + `flower_bed` × 2
    - **clinic**: `a_frame_sign` (x=-20, y=35 診療時間) + `potted_plant` × 2 (x=-20/15, y=32) + `bench` (x=15, y=38)
    - **post_office**: `mailbox` × 3 facade (x=90/110/130, y=35) + `flag_pole` (x=160, y=38) + `bench` (x=80, y=45)
- **境界**: `wood_fence` 園庭周囲 (x=-165, y=50/95)、`hedge` clinic-post 分離 (x=35/60/85, y=95)、`flower_planter_row` y=98 全幅 4 個 (x=-120/-30/40/130)
- **動線**: `pedestrian_bridge` avenue 跨ぎ y=60、`stone_pavement` 歩道 x=-65、**`_TOP_HR` y=200 全幅**

**アンビエント 2 セル**:

- **SW**: `residential_wood` (x=-100, y=130) + `laundry_on_wire` (x=-140, y=160) + `cat` (x=-170, y=180)
- **SE**: `cottage_2f` (x=100, y=130) + 傾き `bicycle` (x=140, y=145) + `trash_bag` (x=170, y=185)

**取り巻き 3 パターン**:

- **facade 沿い**: daycare 園庭具 + clinic sign/bench + post mailbox/flag/bench（建物別クラスター）
- **焦点囲み**: 園庭内に slide/swing_set/flower_bed を集約
- **歩道沿い**: x=-65 `stone_pavement` + `street_lamp` × 2 + `sakura_tree` 西列 × 3 / 東列 × 2、y=200 付近は `_TOP_HR` 自体で装飾

**人配置**: 園児 × 4 園庭 (x=-130/-100/-80/-140 の y=70-90)、保育士 × 1 (x=-110, y=65 園児向き)、post 前老人 (x=110, y=40)、clinic bench 老人 (x=15, y=40)、pedestrian_bridge 上に園児+保護者 (x=0/-5, y=60-62)、`cat` × 2

**地面パッチ**:

- 基調: `residential_tile`
- daycare 園庭: `dirt` (x=-110 y=75, 100×50)
- clinic/post 玄関前: `concrete` 小パッチ × 2 (x=0/110, y=40, 30×15)
- **Act II 予兆**: SE y=185 `concrete` 小パッチ (140, 185, 40×20) → Act II 基調先取り
- 歩道帯: `stone_pavement` (x=-65 y=100, 12×200)

**道路**: `_SPINE_V` + `_MID_HR` + **`_TOP_HR`**（y=200, -180〜+180, 14 厚）

**生活の痕跡 / スケール / 向き**:

- **痕跡**: 園庭 `trash_bag` (x=-90, y=93 忘れ物的)、clinic 前傾き `bicycle` (x=-10, y=45)、post `mailbox` 1 つだけ flag_up、SE 傾き `bicycle`、SW `laundry`
- **スケール**: 大 `daycare`（園庭込み）・`pedestrian_bridge` / 中 `flag_pole`・`slide`・`swing_set` / 小 `mailbox` × 3・`bench` × 2・`potted_plant`・`a_frame_sign` ≈ 25 個
- **向き**: 園庭南向き（日当たり y=50-95）、clinic `a_frame_sign` 道路側、post `flag_pole` 旗は東向き（チャンク内全旗統一）、保育士は園児群を向く

**Handoff (Ch2→Ch3 / Act I→Act II)**:

- **並木**: `sakura_tree` (x=-170, y=190) ↔ Ch3 y=10 樹種 sakura_tree 継続
- **歩道**: `stone_pavement` (x=-65) 維持 → Ch3 で `wood_deck` パッチ切替開始
- **電柱/電線**: `power_pole` (x=178, y=195) + `power_line` y=195 連続
- **予兆 (Act 切替)**: **SE y=185 `concrete` 小パッチ + `potted_plant` 増量** = Act II 小商店基調の先取り。`shop_awning` / `parasol` は Ch3 から登場のため Ch2 には置かない

**TypeScript スケルトン**:

```tsx
const CH2: ChunkTemplate = {
  title: '公共 3 連と歩道橋',
  buildings: [
    _B('daycare', -110, 22),
    _B('clinic', 0, 22),
    _B('post_office', 110, 22),
    _B('kominka', -100, 130),
    _B('house', 100, 130),
  ],
  furnitures: [
    // ── daycare 園庭（NW 内）──
    _F('swing_set', -140, 70), _F('slide', -80, 80), _F('sandbox', -120, 85),
    _F('flower_bed', -100, 50), _F('flower_bed', -130, 90),
    _F('wood_fence', -165, 50), _F('wood_fence', -165, 90),
    _F('trash_bag', -90, 93),
    // ── clinic (中央 facade) ──
    _F('a_frame_sign', -20, 35),
    _F('potted_plant', -20, 32), _F('potted_plant', 15, 32),
    _F('bench', 15, 38),
    _F('bicycle', -10, 45),
    // ── post_office (NE facade) ──
    _F('mailbox', 90, 35), _F('mailbox', 110, 35), _F('mailbox', 130, 35),
    _F('flag_pole', 160, 38),
    _F('bench', 80, 45),
    // ── clinic-post 分離 hedge ──
    _F('hedge', 35, 95), _F('hedge', 60, 95), _F('hedge', 85, 95),
    // ── 境界 / 動線 ──
    _F('flower_planter_row', -120, 98), _F('flower_planter_row', -30, 98),
    _F('flower_planter_row', 40, 98), _F('flower_planter_row', 130, 98),
    _F('pedestrian_bridge', 0, 60),
    // ── SW アンビエント ──
    _F('laundry_pole', -140, 160),
    _F('cat', -170, 180),
    _F('potted_plant', -80, 145), _F('potted_plant', -130, 145),
    // ── SE アンビエント（Act II 予兆増量）──
    _F('bicycle', 140, 145),
    _F('trash_bag', 170, 185),
    _F('potted_plant', 80, 145), _F('potted_plant', 130, 145), _F('potted_plant', 160, 170),
    // ── 歩道沿い ──
    _F('street_lamp', -70, 60), _F('street_lamp', -70, 160),
    _F('sakura_tree', -170, 30), _F('sakura_tree', -170, 100), _F('sakura_tree', -170, 190),
    _F('sakura_tree', 170, 30), _F('sakura_tree', 170, 100),
    _F('power_pole', -178, 195), _F('power_pole', 178, 195), _F('power_line', -90, 195),
  ],
  humans: [
    _H(-130, 85), _H(-100, 80), _H(-80, 90), _H(-140, 75), // 園児 × 4
    _H(-110, 65),                                          // 保育士
    _H(110, 40), _H(15, 40),                               // post 前老人 + clinic bench 老人
    _H(0, 60), _H(-5, 62),                                 // pedestrian_bridge 上
  ],
  grounds: [
    _G('residential_tile', 0, 100, 360, 200),
    _G('dirt', -110, 75, 100, 50),
    _G('concrete', 0, 40, 30, 15), _G('concrete', 110, 40, 30, 15),
    _G('concrete', 140, 185, 40, 20), // Act II 予兆パッチ
    _G('stone_pavement', -65, 100, 12, 200),
  ],
  horizontalRoads: [_MID_HR],
  verticalRoads: [_AVE],
};
```

## Act II — 生活と小商店

### Ch3: 朝のカフェテラス（3 連テラス）

**ヒーロー / アンビエント**: **SW+SE マージ = 大ヒーロー**（bakery + bookstore + cafe クラスター C / 下段 facade y=118）/ NW・NE = アンビエント（住宅 + 並木）

**SW+SE 大ヒーロー 4 層**:

- **焦点 (3 連店舗)**: `bakery` (x=-110, y=118) + `bookstore` (x=0, y=118) + `cafe` (x=110, y=118)
- **取り巻き（共通帯化、§4.7 特例）**:
    - **共通 `shop_awning`** × 3 (x=-110/0/110, y=128) — 3 棟連続 1 帯と扱う
    - **共通 `parasol` 列** × 4 (x=-130/-40/40/130, y=148) — テラス前一列
    - **共通 `bench` × 3** (x=-90/0/100, y=155) — 各店前高さ揃え、微妙に X ズラシ
    - **`a_frame_sign` × 3** (x=-105/-3/108, y=135) — 各店の前側、道路向き向き
    - **`bicycle_row`** (x=-130, y=175) 下段端、傾き 1 台あり
    - **`flower_planter_row` × 2** (x=-160/160, y=115) 帯端
- **境界**: `power_pole` 両端 (x=-178/+178, y=195)、`wood_deck` 舚装の端が光泽境界
- **動線**: **`wood_deck` 帯** 3 店連続 (x=0, y=140, 300×50)、y=108 歩行者帯と MID_HR の間割前

**アンビエント 2 セル**:

- **NW**: `residential_modern` (x=-100, y=20) + `mailbox` (x=-130, y=22) + 傾き `bicycle` (x=-60, y=45) + `potted_plant` × 2
- **NE**: `cottage_2f` (x=100, y=20) + `laundry_on_wire` (x=140, y=55) + `cat` (x=170, y=45) + `potted_plant` × 2

**取り巻き 3 パターン**:

- **facade 沿い** (y=118 帯): `shop_awning` × 3 / `a_frame_sign` × 3 — 3 棟共通扱い
- **焦点囲み**: `parasol` × 4 + `bench` × 3 弧・準対称 (各 X を 2-5 px ずらす)
- **歩道沿い**: y=108 交通帯 + x=-65 歩道帯は Ch3 で `wood_deck` へ幅広く切替 (Act II 基調)

**人配置**: 買い物客 × 2 bakery 前 (x=-110/-100, y=130 商品方向)、カフェ客 × 2 `parasol` 下テーブル (x=120/140, y=148)、bench 読書客 × 1 (x=0, y=155)、歩道歩行者 × 2 (x=-65, y=60-70)、`cat` × 1 NE 店裏

**地面パッチ**:

- 基調: `residential_tile` (NW/NE アンビエント) + `concrete` (下段 Act II 基調)
- 焦点帯: **`wood_deck` 3 棟連続帯** (x=0 y=140, 300×50) — 基本形 C 特例
- 歩道帯: `stone_pavement` は Ch3 で y=0-50 までに終準、代わりに `wood_deck` (x=-65 y=75, 12×70) で Act II 歩道に接続

**道路**: `verticalRoads: [_AVE]` / `horizontalRoads: [_MID_HR]`（追加なし）

**生活の痕跡 / スケール / 向き**:

- **痕跡**: 傾き `bicycle` NW、`laundry_on_wire` NE、`fallen_leaves` 店前小パッチ (x=-140, y=160)、half-open `noren` は Ch4 からなので Ch3 は `shop_awning` の垂れをコメント表現
- **スケール**: 大 `bakery`・`bookstore`・`cafe`（3 棟一体）/ 中 `shop_awning` × 3・`parasol` × 4 / 小 `bench` × 3・`a_frame_sign` × 3・`potted_plant`・`mailbox` ≈ 25 個
- **向き**: `bench` 3 つとも facade に背、道路向き（街を眺める朝食客）、`a_frame_sign` 道路側正面、`parasol` は朝日（東向き）全開、`cat` は店裏側

**Handoff (Ch3→Ch4)**:

- **並木**: `sakura_tree` (x=-170/+170, y=190) ↔ Ch4 y=10 継続
- **歩道**: `wood_deck` パッチを Ch3 下段で縮小、Ch4 では再び `concrete` + 部分 `wood_deck`
- **電柱/電線**: `power_pole` (x=178, y=195) + `power_line` → Ch4
- **予兆**: Ch4 銭湯の `bathhouse_chimney` は Ch3 以外の上方には予兆を置かず、Ch4 から単発登場。Ch3 下段は清らかに維持

**TypeScript スケルトン**:

```tsx
const CH3: ChunkTemplate = {
  title: '朝のカフェテラス（3 連）',
  buildings: [
    _B('bakery', -110, 118),
    _B('bookstore', 0, 118),
    _B('cafe', 110, 118),
    _B('townhouse', -100, 20),
    _B('house', 100, 20),
  ],
  furnitures: [
    // ── 3 連テラス facade （共通帯化）──
    _F('shop_awning', -110, 128), _F('shop_awning', 0, 128), _F('shop_awning', 110, 128),
    _F('a_frame_sign', -105, 135), _F('a_frame_sign', -3, 135), _F('a_frame_sign', 108, 135),
    // ── parasol + bench 弧（3 店共通テラス）──
    _F('parasol', -130, 148), _F('parasol', -40, 148), _F('parasol', 40, 148), _F('parasol', 130, 148),
    _F('bench', -90, 155), _F('bench', 0, 155), _F('bench', 100, 155),
    // ── 駐輪 + 端の装飾 ──
    _F('bicycle_row', -130, 175),
    _F('flower_planter_row', -160, 115), _F('flower_planter_row', 160, 115),
    _F('fallen_leaves', -140, 160),
    // ── NW アンビエント ──
    _F('mailbox', -130, 22),
    _F('bicycle', -60, 45),
    _F('potted_plant', -80, 35), _F('potted_plant', -140, 35),
    // ── NE アンビエント ──
    _F('laundry_on_wire', 140, 55),
    _F('cat', 170, 45),
    _F('potted_plant', 50, 35), _F('potted_plant', 140, 35),
    // ── 歩道沿い ──
    _F('street_lamp', -70, 60), _F('street_lamp', -70, 160),
    _F('sakura_tree', -170, 30), _F('sakura_tree', -170, 100), _F('sakura_tree', -170, 190),
    _F('sakura_tree', 170, 30), _F('sakura_tree', 170, 100), _F('sakura_tree', 170, 190),
    _F('power_pole', -178, 195), _F('power_pole', 178, 195), _F('power_line', -90, 195),
  ],
  humans: [
    _H(-110, 130), _H(-100, 128),  // bakery 前買い物客
    _H(120, 148), _H(140, 146),    // cafe parasol テーブル
    _H(0, 155),                    // bookstore bench 読書客
    _H(-65, 60), _H(-65, 70),      // 歩行者 × 2
  ],
  grounds: [
    _G('residential_tile', 0, 50, 360, 100),  // 上段はまだ Act I 遠像
    _G('concrete', 0, 150, 360, 100),         // 下段 Act II 基調
    _G('wood_deck', 0, 140, 300, 50),         // 3 棟連続テラス帯
    _G('wood_deck', -65, 75, 12, 70),         // 歩道 Act II に切替
    _G('stone_pavement', -65, 20, 12, 40),    // 残しの Act I 歩道（上段）
  ],
  horizontalRoads: [_MID_HR],
  verticalRoads: [_AVE],
};
```

### Ch4: 銭湯とスーパー

**ヒーロー / アンビエント**: **NW = ヒーロー**（銭湯 single-cell B）/ NE・SW・SE = アンビエント（supermarket + pharmacy 月用品店 + 住宅）

**NW ヒーロー 4 層**:

- **焦点**: `bathhouse` (x=-100, y=22) + `bathhouse_chimney` (x=-60, y=8) — 照が右上へびろんと風で入る想定
- **取り巻き（facade 沿い）**: `noren` (x=-100, y=32 half-open) + `milk_crate_stack` (x=-150, y=55) + `a_frame_sign` (x=-75, y=35 入浴料) + 自販機 `vending_machine` × 2 (x=-145/-125, y=38, 壁背)
- **境界**: `flower_planter_row` × 2 (x=-160/-40, y=98) + `power_pole` (x=-178, y=92)
- **動線**: `wood_deck` 銭湯玩関パッチ (x=-100, y=48, 50×15)、常連ベンチ (x=-80, y=58, 玄関向き)

**アンビエント 3 セル**:

- **NE**: `supermarket` (x=100, y=22) + `shop_awning` (x=100, y=32) + `a_frame_sign` (x=85, y=38 セール) + `bicycle_row` (x=130, y=50) + `potted_plant` × 2
- **SW**: `pharmacy` (x=-100, y=118) + `a_frame_sign` (x=-115, y=135) + `potted_plant` × 2 (x=-85/-120, y=128)
- **SE**: `residential_wood` (x=100, y=130) + `laundry_on_wire` (x=140, y=160) + `cat` (x=170, y=175) + `trash_bag` (x=155, y=188)

**取り巻き 3 パターン**:

- **facade 沿い** (y=22/118): 銭湯 `noren` + `a_frame_sign`、supermarket `shop_awning`、pharmacy `a_frame_sign` — 建物別クラスター
- **焦点囲み**: 銭湯の `milk_crate_stack`・常連 bench・`vending_machine` × 2を NW に集約
- **歩道沿い**: x=-65 `wood_deck` 歩道 + `street_lamp` × 2 + supermarket 前 `bicycle_row`、y=108 に `newspaper_stand` (x=50, y=108 avenue 脂)

**人配置**: 銭湯常連老人 × 2 bench (x=-80/-72, y=58 玄関向き)、supermarket 前買い物客 × 3 (x=90/110/130, y=45-50 商品向き)、pharmacy 前子連れ客 (x=-105, y=130)、歩行者 (x=-65, y=70)、`cat` × 2 (NW 銭湯裏 x=-150 y=88、SE x=170 y=175)

**地面パッチ**:

- 基調: `concrete` (Act II 基調) + `residential_tile` (SE 住宅側 x=100〜180 y=100〜200)
- NW 焦点: `wood_deck` 銭湯玄関パッチ (x=-100, y=48, 50×15)
- NE/SW 店前: `wood_deck` 小パッチ × 2 (x=100, y=45, 40×15) (x=-100, y=140, 40×15)
- 歩道帯: `wood_deck` (x=-65 y=100, 12×200)

**道路**: `_SPINE_V` + `_MID_HR` + **部分幅小道 `_HR(170, -180, -30, 'service')`** — 銭湯裏の路地を暗示する背面帯

**生活の痕跡 / スケール / 向き**:

- **痕跡**: `milk_crate_stack` が銭湯横に積み上げ (x=-150, y=55 客たり後)、`noren` half-open コメント指定、`laundry_on_wire` SE、傾き 1 台 `bicycle_row` NE、SE `trash_bag`、NW `fallen_leaves` 小パッチ (x=-170, y=75)
- **スケール**: 大 `bathhouse`・`supermarket` / 中 `bathhouse_chimney`・`shop_awning` / 小 `milk_crate_stack`・`noren`・`a_frame_sign` × 3・自販機 × 2・`potted_plant`·`vending_machine` ≈ 25 個
- **向き**: 銭湯常連 bench 2 つ玄関向き、`vending_machine` は壁背（店側に背、客は道路向きに立つ）、`a_frame_sign` は道路側正面、`laundry_on_wire` は南向き（御台所 y=100 側）、`noren` は half-open で湯煓風表現

**Handoff (Ch4→Ch5)**:

- **並木**: `sakura_tree` (x=-170/+170, y=190) ↔ Ch5 y=10 最終継続（桁切りは Ch5→Ch6）
- **歩道**: `wood_deck` 維持 → Ch5 で `stone_pavement` ・ `plaza_tile_circle` に切替
- **電柱/電線**: `power_pole` (x=-178, y=92) + `power_line` y=195 連続
- **予兆**: Ch5 civic plaza の予兆として SE y=185 に `flag_pole` 小型 × 1 (x=140, y=195) を仕込んで公共感を傱かに出す

**TypeScript スケルトン**:

```tsx
const CH4: ChunkTemplate = {
  title: '銭湯とスーパー',
  buildings: [
    _B('onsen_inn', -100, 22),
    _B('supermarket', 100, 22),
    _B('pharmacy', -100, 118),
    _B('kominka', 100, 130),
  ],
  furnitures: [
    // ── NW 焦点: 銭湯 ──
    _F('bathhouse_chimney', -60, 8),
    _F('noren', -100, 32),
    _F('a_frame_sign', -75, 35),
    _F('milk_crate_stack', -150, 55),
    _F('vending', -145, 38), _F('vending', -125, 38),
    _F('bench', -80, 58), _F('bench', -72, 58),
    _F('flower_planter_row', -160, 98), _F('flower_planter_row', -40, 98),
    _F('fallen_leaves', -170, 75),
    _F('cat', -150, 88),
    // ── NE アンビエント: supermarket ──
    _F('shop_awning', 100, 32),
    _F('a_frame_sign', 85, 38),
    _F('bicycle_row', 130, 50),
    _F('potted_plant', 75, 35), _F('potted_plant', 140, 35),
    // ── SW アンビエント: pharmacy ──
    _F('a_frame_sign', -115, 135),
    _F('potted_plant', -85, 128), _F('potted_plant', -120, 128),
    // ── SE アンビエント: 住宅 ──
    _F('laundry_on_wire', 140, 160),
    _F('cat', 170, 175),
    _F('trash_bag', 155, 188),
    _F('potted_plant', 80, 145), _F('potted_plant', 130, 145),
    // ── Ch5 予兆 (flag_pole 小) ──
    _F('flag_pole', 140, 195),
    // ── 歩道沿い ──
    _F('street_lamp', -70, 60), _F('street_lamp', -70, 160),
    _F('newspaper_stand', 50, 108),
    _F('sakura_tree', -170, 30), _F('sakura_tree', -170, 100), _F('sakura_tree', -170, 190),
    _F('sakura_tree', 170, 30), _F('sakura_tree', 170, 100), _F('sakura_tree', 170, 190),
    _F('power_pole', -178, 92), _F('power_pole', 178, 92),
    _F('power_pole', -178, 195), _F('power_pole', 178, 195), _F('power_line', -90, 195),
  ],
  humans: [
    _H(-80, 58), _H(-72, 58),        // 銭湯常連
    _H(90, 45), _H(110, 50), _H(130, 48),  // supermarket 前
    _H(-105, 130),                   // pharmacy 前
    _H(-65, 70),                     // 歩行者
  ],
  grounds: [
    _G('concrete', 0, 100, 360, 200),
    _G('residential_tile', 140, 150, 80, 100),
    _G('wood_deck', -100, 48, 50, 15),
    _G('wood_deck', 100, 45, 40, 15),
    _G('wood_deck', -100, 140, 40, 15),
    _G('wood_deck', -65, 100, 12, 200),
  ],
  roads: [..._SPINE_V, _MID_HR, _HR(170, -180, -30, 'service')],
};
```

### Ch5: civic plaza（Act II 末・クライマックス開始）

**ヒーロー / アンビエント**: **SW+SE マージ = 大ヒーロー**（civic plaza オープンスペース A / 下段全幅 360×100）/ NW = post_office (Act I の `post_office` と異なる本分館） single / NE = bank single

**SW+SE 大ヒーロー 4 層**:

- **焦点**: `plaza_tile_circle` (x=2, y=150, 大径 140) + `statue` (x=2, y=148 avenue 向き) + `fountain` (x=2, y=165, 像直後の整備霧水)
- **取り巻き（焦点囲み、弧·準対称）**: `flag_pole` × 2 (x=-40/45, y=132 旗東向き統一) + `bench` × 3 (x=-60/2/65, y=175 像向き) + `flower_bed` × 4 (x=-80/-25/30/85, y=185) + `street_lamp` × 4 (x=-120/-40/45/125, y=155 四隅)
- **境界**: `flower_planter_row` × 4 (x=-150/-80/80/150, y=108) 上縁 + `power_pole` 両端 (x=-178/+178, y=195)
- **動線**: `stone_pavement` 広場全面 (x=0, y=150, 360×100) + `plaza_tile_circle` が中心円 + avenue 入口 (y=100) は広く開ける

**アンビエント 2 セル**（NW・NE は上段に略 1 棟ずつ、廃富を持つ）:

- **NW**: `post_office` (x=-100, y=22 本分館、Ch2 より大く) + `mailbox` × 3 (x=-130/-100/-70, y=35 不均等) + `flag_pole` (x=-45, y=38) + `bench` (x=-115, y=45)
- **NE**: `bank` (x=100, y=22) + `flag_pole` (x=55, y=38) + `potted_plant` × 3 (x=75/130/160, y=32) + `bench` (x=120, y=45)

**取り巻き 3 パターン**:

- **facade 沿い** (y=22): post の `mailbox` 帯 + 両端 `flag_pole`
- **焦点囲み**: plaza に `flag_pole` × 2 + `bench` × 3 + `flower_bed` × 4 + `street_lamp` × 4 → 弧状 + 準対称
- **歩道沿い**: x=-65 `stone_pavement` 帯 → Ch5 で広場座と合流して既に歩道帯の区別が消える (`stone_pavement` 全面化)

**人配置**: 公園散歩老人 × 2 bench (x=-60/65, y=175 像向き)、像前見学客 × 2 (x=2/-10, y=152 像向き)、fountain 前観光客 (x=30, y=170)、post 前お客 (x=-115, y=42)、bank 前スーツ客 (x=120, y=42)、`cat` × 1 (x=170, y=60 bank 裏)

**地面パッチ**:

- 基調: `concrete` (上段 Act II) + `stone_pavement` (下段広場 Act II → Act III civic 格式)
- SW+SE 焦点: **`plaza_tile_circle`** (x=2, y=150, 140×140) + `stone_pavement` 広場全幅 (x=0, y=150, 360×100)
- 歩道帯: Ch5 は歩道と広場が連続化するため別挙なし

**道路**: `_SPINE_V` + `_MID_HR`（追加なし、道路セグメントは維持、地面パッチで広場化）

**生活の痕跡 / スケール / 向き**:

- **痕跡**: fountain の `puddle_reflection` 周囲 × 1 (x=2, y=178 噴水の萃り)、`fallen_leaves` 広場隅 × 2 (x=-170/+170, y=170)、bank 裏 `potted_plant` 不揃い、post の `mailbox` 1 つだけ flag_up (x=-100, y=35)、NW 傾き `bicycle` (x=-60, y=45)
- **スケール**: 大 `plaza_tile_circle`（140×140 大パッチ） / 中 `statue`・`fountain`・`flag_pole` × 2・`post_office`・`bank` / 小 `bench` × 3・`flower_bed` × 4・`street_lamp` × 4・`potted_plant`・`mailbox` × 3 ≈ 30 個
- **向き**: `statue` は avenue (y=100) 向き、`bench` 3 つ全て像向き（civic plaza の規約）、`flag_pole` 旗は東向き統一、`fountain` 噴水は上向き、`cat` は店裏側

**Handoff (Ch5→Ch6 / Act II→Act III)**:

- **並木 (重要切替)**: `sakura_tree` は **Ch5 y=200 で終端**。切替地点 x=-170/+170, y=200 で sakura_tree 最後の一本、Ch6 y=0 から `tree` に切替。x 座標は同じで樹種だけ変わる
- **歩道**: `stone_pavement` 広場 → Ch6 で `stone_pavement` 校門前 + `dirt` 校庭に分岳
- **電柱/電線**: `power_pole` (x=178, y=195) + `power_line` → Ch6
- **予兆 (Act 切替)**: Ch6 小学校の `flag_pole` はすでに Ch5 に 2 本登場済みなので、加えて Ch5 下段東端に `tree` 小型 1 本 (x=175, y=195) を仮置きして並木切替を予定調和

**TypeScript スケルトン**:

```tsx
const CH5: ChunkTemplate = {
  title: 'civic plaza',
  buildings: [
    _B('post_office', -100, 22),
    _B('bank', 100, 22),
  ],
  furnitures: [
    // ── SW+SE plaza 焦点 ──
    _F('statue', 2, 148),
    _F('fountain', 2, 165),
    _F('flag_pole', -40, 132), _F('flag_pole', 45, 132),
    _F('bench', -60, 175), _F('bench', 2, 175), _F('bench', 65, 175),
    _F('flower_bed', -80, 185), _F('flower_bed', -25, 185),
    _F('flower_bed', 30, 185), _F('flower_bed', 85, 185),
    _F('street_lamp', -120, 155), _F('street_lamp', -40, 155),
    _F('street_lamp', 45, 155), _F('street_lamp', 125, 155),
    _F('flower_planter_row', -150, 108), _F('flower_planter_row', -80, 108),
    _F('flower_planter_row', 80, 108), _F('flower_planter_row', 150, 108),
    _F('puddle_reflection', 2, 178),
    _F('fallen_leaves', -170, 170), _F('fallen_leaves', 170, 170),
    // ── NW: post_office 本分館 ──
    _F('mailbox', -130, 35), _F('mailbox', -100, 35), _F('mailbox', -70, 35),
    _F('flag_pole', -45, 38),
    _F('bench', -115, 45),
    _F('bicycle', -60, 45),
    _F('potted_plant', -140, 32), _F('potted_plant', -45, 30),
    // ── NE: bank ──
    _F('flag_pole', 55, 38),
    _F('potted_plant', 75, 32), _F('potted_plant', 130, 32), _F('potted_plant', 160, 32),
    _F('bench', 120, 45),
    _F('cat', 170, 60),
    // ── 歩道沿い ──
    _F('street_lamp', -70, 60), _F('street_lamp', -70, 98),
    _F('sakura_tree', -170, 30), _F('sakura_tree', -170, 90), _F('sakura_tree', -170, 200),
    _F('sakura_tree', 170, 30), _F('sakura_tree', 170, 90), _F('sakura_tree', 170, 200),
    // ── Ch6 予兆 (tree 小型 1 本) ──
    _F('tree', 175, 195),
    _F('power_pole', -178, 195), _F('power_pole', 178, 195), _F('power_line', -90, 195),
  ],
  humans: [
    _H(-60, 175), _H(65, 175),      // 広場 bench 散歩老人
    _H(2, 152), _H(-10, 152),       // 像前見学客
    _H(30, 170),                    // fountain 前観光客
    _H(-115, 42),                   // post 客
    _H(120, 42),                    // bank 客
  ],
  grounds: [
    _G('concrete', 0, 50, 360, 100),         // 上段 Act II 基調
    _G('stone_pavement', 0, 150, 360, 100),  // 下段広場
    _G('plaza_tile_circle', 2, 150, 140, 140),
  ],
  horizontalRoads: [_MID_HR],
  verticalRoads: [_AVE],
};
```

## Act III — ローカル商店街

### Ch6: 小学校キャンパス（全セルマージ / Act III 頭 / 最大チャンク）

**ヒーロー / アンビエント**: **全セルマージ = 1 大ヒーロー**（小学校キャンパス クラスター C / 360×200 全面）/ アンビエントセルなし（§3.6 マージチャンク例外）

**全セル 4 層**:

- **焦点 (校舎 + 校庭)**: `school` (x=0, y=22) 校舎 + `flag_pole` × 2 (x=-100/100, y=38 校門、大型旗乱病方向一致)  + `statue` (x=-130, y=60 二宮翁像、校舎向き) + 校庭 `play_structure` (x=0, y=145) + `swing_set` (x=-80, y=145) + `slide` (x=90, y=145) + `sandbox` (x=-130, y=175) + `koi_pond` (x=130, y=170 プール代用、水面表現)
- **取り巻き（校舎・校庭）**: 校舎前 `flower_planter_row` × 4 (x=-150/-50/50/150, y=45)、`bench` × 3 (x=-60/0/60, y=125 遅刻保護者待機)、`flower_bed` × 2 (x=-60/60, y=180 校庭隅)、`street_lamp` × 4 (x=-140/-40/45/140, y=135 四隅)
- **境界**: `wood_fence` 校地外周 6 箇所 (x=-170/-170/170/170, y=8/195 + x=-100/100, y=198 校門) + 裏山 `pine_tree` × 5 (x=-175/-160/-145/-130/-115, y=5-10) ← Act IV 鄕外感予兆
- **動線**: 校門前 `stone_pavement` パッチ (x=0, y=55, 100×20) + 校庭 `dirt` 大パッチ (x=0, y=160, 280×70) + **`_TOP_HR`** y=200 全幅

**アンビエント (merged 例外)**: 全セルマージのため独立アンビエントセル無し。校舎・校庭が 4 セル全てを占有（§3.6 マージチャンク例外）。

**取り巻き 3 パターン**:

- **facade 沿い** (y=22): school 校舎に `flower_planter_row` × 4 と `flag_pole` × 2 (校門)
- **焦点囲み**: 校庭に `play_structure`・`swing_set`・`slide`・`sandbox`・`koi_pond`・`bench` × 3・`flower_bed` × 2・`street_lamp` × 4・`statue` = 焦点集約
- **歩道沿い**: x=-65 は校門前広場と融合して `stone_pavement` 帯が横に広がる（y=55 に横帯 100×20、42 歩道側は縮小）、既存の y=100 avenue 歩者帯に `newspaper_stand` (x=70, y=108)

**人配置**: 下校小学生 × 4 校庭遊具上 (x=0/90/-80/130, y=145-175 遊具方向)、教師 (x=-5, y=125 bench 前、児童方向)、保護者 × 2 bench (x=-60/60, y=125)、下校小学生归宅群 × 3 校門前 (x=-10/5/15, y=60 avenue 向き)、通勤客 × 2 歩道 (x=-65, y=95/105)、`cat` × 1 校庭隅 (x=170, y=180)、`cat` × 1 裏山 (x=-175, y=15)

**地面パッチ**:

- 基調: `concrete` (Act III 基調)
- 校庭焦点: **`dirt` 大パッチ** (x=0, y=160, 280×70) = 小学校の標識地面
- 校門前: `stone_pavement` (x=0, y=55, 100×20)
- 裏山: `grass` 小パッチ (x=-145, y=10, 80×20) + `fallen_leaves` 小パッチ (x=-150, y=22, 60×10)
- 歩道帯: `stone_pavement` (x=-65 y=100, 12×200)

**道路**: `_SPINE_V` + `_MID_HR` + **`_TOP_HR`**（y=200、-180〜+180、14 厚、既存要素）

**生活の痕跡 / スケール / 向き**:

- **痕跡**: 校庭 `sandbox` 周辺に `fallen_leaves` 小パッチ (x=-130, y=180)、校門前 `bicycle_row` (x=-140, y=55 敷商生忘れ物)、`cat` × 2、裏山 `fallen_leaves` = 秋感、小学生の端くしづ文具 (`milk_crate_stack` はないので `trash_bag` (x=150, y=195 校庭隅))
- **スケール**: 大 `school` (全幅級) / 中 `flag_pole` × 2・`statue`・`pine_tree` × 5・校庭遊具群 / 小 `bench` × 3・`flower_bed` × 2・`flower_planter_row` × 4・`street_lamp` × 4・`potted_plant`・`newspaper_stand` ≈ 32 個
- **向き**: `statue` 二宮像は校舎向き (読書の方向)、`flag_pole` 旗は東向き統一、`bench` 3 つ保護者席は校庭向き（児童方向）、`swing_set`・`slide` は南向き、小学生群は avenue 向きに歩き出す

**Handoff (Ch6→Ch7)**:

- **並木**: `tree` (x=-170/+170, y=190) ↔ Ch7 y=10 で `tree` 継続（Ch5→Ch6 で sakura → tree 切替済み、Ch7 も tree）
- **歩道**: `stone_pavement` → Ch7 で商店街店前 `wood_deck`/`tile` パッチに変化
- **電柱/電線**: `power_pole` (x=-178/+178, y=195) + `power_line` → Ch7 へ
- **予兆**: Ch7 提灯アーケードの予兆として Ch6 下段単独の `banner_pole` × 1 (x=60, y=190 校庭隅の運動会旗竿残像) を仕込んで商店街の示唆竿を示唆

**TypeScript スケルトン**:

```tsx
const CH6: ChunkTemplate = {
  title: '小学校キャンパス',
  buildings: [
    _B('school', 0, 22),
  ],
  furnitures: [
    // ── 校門 ──
    _F('flag_pole', -100, 38), _F('flag_pole', 100, 38),
    _F('statue', -130, 60),
    // ── facade 前装飾 ──
    _F('flower_planter_row', -150, 45), _F('flower_planter_row', -50, 45),
    _F('flower_planter_row', 50, 45), _F('flower_planter_row', 150, 45),
    _F('bicycle_row', -140, 55),
    // ── 校庭遊具群 ──
    _F('play_structure', 0, 145),
    _F('swing_set', -80, 145),
    _F('slide', 90, 145),
    _F('sandbox', -130, 175),
    _F('koi_pond', 130, 170),  // プール代用
    // ── 校庭取り巻き ──
    _F('bench', -60, 125), _F('bench', 0, 125), _F('bench', 60, 125),
    _F('flower_bed', -60, 180), _F('flower_bed', 60, 180),
    _F('street_lamp', -140, 135), _F('street_lamp', -40, 135),
    _F('street_lamp', 45, 135), _F('street_lamp', 140, 135),
    _F('fallen_leaves', -130, 180),
    _F('trash_bag', 150, 195),
    // ── 裏山 pine ──
    _F('pine_tree', -175, 5), _F('pine_tree', -160, 8), _F('pine_tree', -145, 5),
    _F('pine_tree', -130, 8), _F('pine_tree', -115, 5),
    _F('fallen_leaves', -150, 22),
    _F('cat', -175, 15),
    // ── 校地四周フェンス + 校門 ──
    _F('wood_fence', -170, 8), _F('wood_fence', 170, 8),
    _F('wood_fence', -170, 195), _F('wood_fence', 170, 195),
    _F('wood_fence', -100, 198), _F('wood_fence', 100, 198),
    // ── 歩道沿い ──
    _F('street_lamp', -70, 60), _F('street_lamp', -70, 160),
    _F('newspaper_stand', 70, 108),
    _F('tree', -170, 30), _F('tree', -170, 100), _F('tree', -170, 190),
    _F('tree', 170, 30), _F('tree', 170, 100), _F('tree', 170, 190),
    // ── Ch7 予兆（旗竿残像）──
    _F('banner_pole', 60, 190),
    _F('cat', 170, 180),
    _F('power_pole', -178, 195), _F('power_pole', 178, 195), _F('power_line', -90, 195),
  ],
  humans: [
    _H(0, 145), _H(90, 145), _H(-80, 145), _H(130, 175),  // 小学生 × 4
    _H(-5, 125),                                          // 教師
    _H(-60, 125), _H(60, 125),                            // 保護者 × 2
    _H(-10, 60), _H(5, 60), _H(15, 60),                   // 校門前归宅群
    _H(-65, 95), _H(-65, 105),                            // 歩道通勤客
  ],
  grounds: [
    _G('concrete', 0, 100, 360, 200),
    _G('dirt', 0, 160, 280, 70),
    _G('stone_pavement', 0, 55, 100, 20),
    _G('grass', -145, 10, 80, 20),
    _G('stone_pavement', -65, 100, 12, 200),
  ],
  horizontalRoads: [_MID_HR],
  verticalRoads: [_AVE],
};
```

### Ch7: 提灯アーケード商店街

**ヒーロー / アンビエント**: **NW+NE マージ = 大ヒーロー**（提灯アーケード / avenue 天蓋系 クラスター C）/ SW・SE = アンビエント（路地裏・小食堂）

**NW+NE 大ヒーロー 4 層**:

- **焦点 (商店街 4 店 + 天蓋)**: `greengrocer` (x=-120, y=22) + `fishmonger` (x=-45, y=22) + `butcher` (x=45, y=22) + `liquor_shop` (x=120, y=22) — 食材系の 4 連 facade
- **取り巻き（共通天蓋・§4.7 特例）**:
    - **`chouchin` 14 本** avenue 両脉 (x=-15/15 の 2 列、y=15/30/45/60/75/90/95 × 2) — 天蓋帯
    - **`banner_pole` × 4** (x=-140/-45/45/140, y=10 商店街入口ゲート風)
    - **共通 `shop_awning` × 4** (x=-120/-45/45/120, y=32) — 4 店連続 1 帯
    - **`a_frame_sign` × 4** (x=-115/-40/50/115, y=38)
    - **`parasol` × 3** (x=-80/0/80, y=55 商店街通しの客韱)
    - **`bicycle_rack`** (x=-160, y=50) + **`bicycle_row`** (x=160, y=50) 両端
    - **`flower_planter_row` × 2** (x=-170/170, y=95)
- **境界**: `power_pole` 両端 (x=-178/+178, y=8 上端 + x=-178/+178, y=95 中端) + `wood_fence` y=95 下縁 (x=-60/60 アーケード終端感)
- **動線**: avenue (x=0) 天蓋通路、各店前 `wood_deck` 小パッチ × 4、y=100 交差点は通常通り

**アンビエント 2 セル**:

- **SW**: `residential_wood` (x=-100, y=130) + `laundry_on_wire` (x=-140, y=160) + `cat` (x=-170, y=175) + `trash_bag` (x=-155, y=188)
- **SE**: `ramen` ミニ (x=100, y=130 商店街裏の食堂 — 提灯光源予兆) + `chouchin` × 2 (x=85/115, y=140) + `noren` (x=100, y=142 half-open) + `a_frame_sign` (x=75, y=148)

**取り巻き 3 パターン**:

- **facade 沿い** (y=22 帯): 4 棟共通 `shop_awning` + `a_frame_sign` × 4 + `banner_pole` × 4 = 商店街ゲート感
- **焦点囲み (avenue 天蓋)**: `chouchin` 14 本帯 + `parasol` × 3
- **歩道沿い**: `bicycle_rack`/`bicycle_row` 両端 + y=108 に `newspaper_stand` (x=-30, y=108) + `vending_machine` × 2 (x=-160/160, y=65 壁背)

**人配置**: 商店街客 × 4 各店前 (x=-120/-45/45/120, y=38 商品方向)、avenue 下校小学生群 × 3 (x=-5/5/15, y=70 avenue 向き)、通勤客 × 2 (x=-65, y=55/110)、SE ramen 入店客 (x=100, y=140)、`cat` × 2 (SW・SE)

**地面パッチ**:

- 基調: `concrete` (Act III 基調) 全面
- NW+NE 焦点: `wood_deck` 小パッチ × 4 (x=-120/-45/45/120, y=45, 35×12) — 各店前・基本形 C既例の店に合わせた分割小帯
- y=60 中心に `tile` 小帯 (x=0, y=65, 120×30) — アーケード中心装飾
- 歩道帯: `concrete` 因式のため歩道帯既略（x=-65 線は維持するが色別無し）

**道路**: `_SPINE_V` + `_MID_HR` + **部分幅小道 `_HR(8, -60, 60, 'service')`** — 商店街北端の小道 (帯の上尿)

**生活の痕跡 / スケール / 向き**:

- **痕跡**: `trash_bag` SW・SE、`laundry_on_wire` SW、傾き `bicycle_row` SE (x=155, y=145)、`fallen_leaves` 商店街隅 (x=-165, y=58)、`noren` SE ramen half-open、`chouchin` 1 本だけ限とかした描写コメント
- **スケール**: 大 `greengrocer`・`fishmonger`・`butcher`・`liquor_shop` (4 棟一体) / 中 `chouchin` × 14・`banner_pole` × 4・`shop_awning` × 4・`parasol` × 3・`ramen` / 小 `a_frame_sign` × 4・`potted_plant`・`flower_planter_row`・`bicycle_rack`・`vending_machine` ≈ 35 個
- **向き**: `chouchin` 垂直、帆は風無し（夯暮れ前、`banner_pole` の旗は大旗東向き統一）、`a_frame_sign` 道路側、小学生群は avenue (y=100) 向きに歩く、SE ramen `noren` は half-open で湯気感

**Handoff (Ch7→Ch8)**:

- **並木**: `tree` (x=-170/+170, y=190) ↔ Ch8 y=10 `tree` 継続
- **歩道**: `concrete` → Ch8 駅前 `asphalt` 広場へ切替
- **電柱/電線**: `power_pole` (x=-178/+178, y=95 中端) と y=195 下端 → Ch8
- **予兆**: Ch8 駅商基調 `asphalt` の予兆として Ch7 下段 ramen 店前に点灯後の `puddle_reflection` 小パッチ 1 個 (x=110, y=155)

**TypeScript スケルトン**:

```tsx
const CH7: ChunkTemplate = {
  title: '提灯アーケード',
  buildings: [
    _B('greengrocer', -120, 22),
    _B('fishmonger', -45, 22),
    _B('butcher', 45, 22),
    _B('liquor_shop', 120, 22),
    _B('kominka', -100, 130),
    _B('ramen', 100, 130),
  ],
  furnitures: [
    // ── avenue 天蓋 chouchin 14 本 ──
    _F('chouchin', -15, 15), _F('chouchin', 15, 15),
    _F('chouchin', -15, 30), _F('chouchin', 15, 30),
    _F('chouchin', -15, 45), _F('chouchin', 15, 45),
    _F('chouchin', -15, 60), _F('chouchin', 15, 60),
    _F('chouchin', -15, 75), _F('chouchin', 15, 75),
    _F('chouchin', -15, 90), _F('chouchin', 15, 90),
    _F('chouchin', -15, 95), _F('chouchin', 15, 95),
    // ── banner_pole 入口ゲート ──
    _F('banner_pole', -140, 10), _F('banner_pole', -45, 10),
    _F('banner_pole', 45, 10), _F('banner_pole', 140, 10),
    // ── facade 共通帯 (4 店連続) ──
    _F('shop_awning', -120, 32), _F('shop_awning', -45, 32),
    _F('shop_awning', 45, 32), _F('shop_awning', 120, 32),
    _F('a_frame_sign', -115, 38), _F('a_frame_sign', -40, 38),
    _F('a_frame_sign', 50, 38), _F('a_frame_sign', 115, 38),
    // ── avenue parasol × 3 ──
    _F('parasol', -80, 55), _F('parasol', 0, 55), _F('parasol', 80, 55),
    // ── 両端 駐輪 ──
    _F('bicycle_rack', -160, 50),
    _F('bicycle_row', 160, 50),
    _F('vending', -160, 65), _F('vending', 160, 65),
    // ── 境界 ──
    _F('flower_planter_row', -170, 95), _F('flower_planter_row', 170, 95),
    _F('wood_fence', -60, 95), _F('wood_fence', 60, 95),
    _F('fallen_leaves', -165, 58),
    _F('newspaper_stand', -30, 108),
    // ── SW アンビエント ──
    _F('laundry_pole', -140, 160),
    _F('cat', -170, 175),
    _F('trash_bag', -155, 188),
    _F('potted_plant', -80, 145), _F('potted_plant', -130, 145),
    // ── SE アンビエント: ramen ──
    _F('chouchin', 85, 140), _F('chouchin', 115, 140),
    _F('noren', 100, 142),
    _F('a_frame_sign', 75, 148),
    _F('bicycle_row', 155, 145),
    _F('puddle_reflection', 110, 155),
    _F('cat', 170, 180),
    // ── 歩道沿い ──
    _F('street_lamp', -70, 60), _F('street_lamp', -70, 160),
    _F('tree', -170, 30), _F('tree', -170, 100), _F('tree', -170, 190),
    _F('tree', 170, 30), _F('tree', 170, 100), _F('tree', 170, 190),
    _F('power_pole', -178, 8), _F('power_pole', 178, 8),
    _F('power_pole', -178, 95), _F('power_pole', 178, 95),
    _F('power_pole', -178, 195), _F('power_pole', 178, 195), _F('power_line', -90, 195),
  ],
  humans: [
    _H(-120, 38), _H(-45, 38), _H(45, 38), _H(120, 38),  // 商店街客
    _H(-5, 70), _H(5, 70), _H(15, 70),                   // 下校小学生群
    _H(-65, 55), _H(-65, 110),                           // 通勤客
    _H(100, 140),                                        // ramen 入店客
  ],
  grounds: [
    _G('concrete', 0, 100, 360, 200),
    _G('wood_deck', -120, 45, 35, 12), _G('wood_deck', -45, 45, 35, 12),
    _G('wood_deck', 45, 45, 35, 12), _G('wood_deck', 120, 45, 35, 12),
    _G('tile', 0, 65, 120, 30),
  ],
  roads: [..._SPINE_V, _MID_HR, _HR(8, -60, 60, 'service')],
};
```

### Ch8: 地方の小駅（NE single / Act III 末 / 呼吸チャンク）

**ヒーロー / アンビエント**: **NE = ヒーロー**（train_station single-cell B / 駅前 `asphalt` パッチ）/ NW・SW・SE = アンビエント（駅前小商店・住宅・Act IV 予兆）

**NE ヒーロー 4 層**:

- **焦点**: `train_station` (x=100, y=22 木造風小駅) + `clock_tower` 代用 → `flag_pole` (x=160, y=38 駅名泉代替)
- **取り巻き（facade 沿い）**: `taxi_rank_sign` (x=55, y=38) + `bus_stop` (x=140, y=48) + `bench` × 2 (x=70/140, y=55 駅向き統一) + `newspaper_stand` (x=85, y=48) + `vending_machine` × 2 (x=120/130, y=45 壁背)
- **境界**: `flower_planter_row` × 2 (x=65/155, y=95) + `power_pole` (x=178, y=92) + `tree` (x=175, y=50)
- **動線**: **駅前 `asphalt` 小広場パッチ** (x=100, y=70, 150×40) + `stone_pavement` 小タクシー寄せ (x=55, y=78, 25×15)

**アンビエント 3 セル**:

- **NW**: `stationery_shop` (x=-100, y=22 文房具店) + `a_frame_sign` (x=-115, y=38) + `shop_awning` (x=-100, y=32) + `potted_plant` × 2 (x=-75/-130, y=32)
- **SW**: `residential_wood` (x=-100, y=130) + `laundry_on_wire` (x=-140, y=160) + `cat` (x=-170, y=180) + `potted_plant` × 2
- **SE**: `residential_wood` (x=100, y=130) + `trash_bag` (x=155, y=188) + `potted_plant` × 2 + **`pine_tree` × 3 (x=150/165/175, y=180-190) = Act IV 予兆**

**取り巻き 3 パターン**:

- **facade 沿い** (y=22/118): 駅 `flag_pole` + `bus_stop`、stationery `shop_awning` + `a_frame_sign`
- **焦点囲み**: `bench` × 2・`taxi_rank_sign`・`newspaper_stand`・`vending_machine` × 2 を駅前広場に集約
- **歩道沿い**: x=-65 `stone_pavement` (Ch8 で中段切替、上段は Act III `tile` / 下段は Act III `concrete`)、avenue 脂 y=108 に `mailbox` (x=-30, y=108)

**人配置**: 駅前通勤客束 × 3 (x=85/95/105, y=75 駅向き)、bench 待機客 × 2 (x=70/140, y=58)、taxi 待ち客 (x=55, y=80)、stationery 端客 × 2 (x=-110/-100, y=45)、通勤客 avenue (x=-65, y=50)、`cat` × 2 (SW・NE 裏 x=175, y=35)

**地面パッチ**:

- 基調: `concrete` (Act III 基調)
- NE 焦点: **駅前 `asphalt` 小広場** (x=100, y=70, 150×40) + `stone_pavement` 小タクシー寄せ (x=55, y=78, 25×15)
- NW 店前: `wood_deck` (x=-100, y=45, 40×12)
- SE 予兆パッチ: `grass` 小パッチ (x=160, y=180, 50×25) → `pine_tree` × 3 とセットで Act IV 田園を予兆
- 歩道帯: `tile` (x=-65, y=50, 12×100 上段) + `concrete` 欠所は基調のまま

**道路**: `_SPINE_V` + `_MID_HR`（追加なし・駅前広場は道路セグメントではなく `asphalt` ground で表現）

**生活の痕跡 / スケール / 向き**:

- **痕跡**: 駅前 `trash_bag` (x=120, y=92 サラリーマンの忘れ物)、SW `laundry_on_wire`、SE `trash_bag`、NW 傾き `bicycle_row` (x=-140, y=50 商店生忘れ物)、`fallen_leaves` 駅前隅 (x=170, y=85)
- **スケール**: 大 `train_station`・`stationery_shop` / 中 `pine_tree` × 3・`flag_pole`・`shop_awning`・`bus_stop` / 小 `bench` × 2・`taxi_rank_sign`・`newspaper_stand`・`vending_machine` × 2・`a_frame_sign`・`potted_plant` ≈ 25 個
- **向き**: 駅前 bench 2 つとも駅舎入口向き (y=55 から y=22 を注視)、`taxi_rank_sign` は車道向き、`bus_stop` 標識は道路側、`vending_machine` 壁背、`flag_pole` 旗は東向き統一、通勤客束は駅入口向きに歩く

**Handoff (Ch8→Ch9 / Act III→Act IV)**:

- **並木切替**: `tree` (x=-170, y=190) ↔ Ch9 y=10 `tree` 引き継ぎ、**但し x=+170 は `pine_tree` へ切替** (x=170, y=190) ↔ Ch9 y=10 `pine_tree`。**切替地点 x=+170**。SE の `pine_tree` × 3 (y=180-190) が Ch9 へ自然接続
- **歩道**: `concrete` から Ch9 で `grass`/路肩のみに切替（歩道帯は消滅。Ch8 下段で歩道帯を尊重通りに縮小）
- **電柱/電線**: `power_pole` (x=±178, y=195) + `power_line` → Ch9 継続
- **予兆 (Act 切替)**: SE の **`pine_tree` × 3 + `grass` 小パッチ** = Act IV 田園基調の先行。`greenhouse`/`grain_silo` は Ch9 から登場のため Ch8 には置かない

**TypeScript スケルトン**:

```tsx
const CH8: ChunkTemplate = {
  title: '地方の小駅',
  buildings: [
    _B('train_station', 100, 22),
    _B('stationery_shop', -100, 22),
    _B('kominka', -100, 130),
    _B('kominka', 100, 130),
  ],
  furnitures: [
    // ── NE 焦点: 駅前 ──
    _F('flag_pole', 160, 38),
    _F('taxi_rank_sign', 55, 38),
    _F('bus_stop', 140, 48),
    _F('bench', 70, 55), _F('bench', 140, 55),
    _F('newspaper_stand', 85, 48),
    _F('vending', 120, 45), _F('vending', 130, 45),
    _F('flower_planter_row', 65, 95), _F('flower_planter_row', 155, 95),
    _F('trash_bag', 120, 92),
    _F('fallen_leaves', 170, 85),
    _F('cat', 175, 35),
    // ── NW アンビエント: stationery_shop ──
    _F('shop_awning', -100, 32),
    _F('a_frame_sign', -115, 38),
    _F('bicycle_row', -140, 50),
    _F('potted_plant', -75, 32), _F('potted_plant', -130, 32),
    // ── SW アンビエント ──
    _F('laundry_pole', -140, 160),
    _F('cat', -170, 180),
    _F('potted_plant', -80, 145), _F('potted_plant', -130, 145),
    // ── SE アンビエント (Act IV 予兆) ──
    _F('trash_bag', 155, 188),
    _F('pine_tree', 150, 180), _F('pine_tree', 165, 185), _F('pine_tree', 175, 190),
    _F('potted_plant', 80, 145), _F('potted_plant', 130, 145),
    // ── 歩道沿い ──
    _F('street_lamp', -70, 60), _F('street_lamp', -70, 160),
    _F('mailbox', -30, 108),
    _F('tree', -170, 30), _F('tree', -170, 100), _F('tree', -170, 190),
    _F('tree', 170, 30), _F('tree', 170, 100),
    _F('power_pole', -178, 92), _F('power_pole', 178, 92),
    _F('power_pole', -178, 195), _F('power_pole', 178, 195), _F('power_line', -90, 195),
  ],
  humans: [
    _H(85, 75), _H(95, 75), _H(105, 75),  // 駅前通勤客束
    _H(70, 58), _H(140, 58),              // bench 待機客
    _H(55, 80),                           // taxi 待ち
    _H(-110, 45), _H(-100, 45),           // stationery 客
    _H(-65, 50),                          // 通勤客 avenue
  ],
  grounds: [
    _G('concrete', 0, 100, 360, 200),
    _G('asphalt', 100, 70, 150, 40),      // 駅前広場
    _G('stone_pavement', 55, 78, 25, 15), // taxi 寄せ
    _G('wood_deck', -100, 45, 40, 12),    // stationery 店前
    _G('grass', 160, 180, 50, 25),        // Act IV 予兆
    _G('tile', -65, 50, 12, 100),         // 上段歩道
  ],
  horizontalRoads: [_MID_HR],
  verticalRoads: [_AVE],
};
```

## Act IV — 街はずれ

### Ch9: 田園農家と穀物サイロ（SE ヒーロー、Act IV 頭）

**ヒーロー / アンビエント**: **SE = ヒーロー**（穀物サイロと田の字区画、基本形 B + オープンスペース）/ NW・NE・SW = アンビエント（農家・空き地）

**SE ヒーロー 4 層**:

- **焦点**: `grain_silo` (x=110, y=118) ★ ランドマーク + `greenhouse` × 2 (x=150, y=135/175) ビニールハウス
- **取り巻き（焦点囲み）**: `wood_fence` 田の字畔道 × 4 (x=-40/+20, y=140/175)、`puddle_reflection` × 2 (x=-60/+60, y=110)、`potted_plant` × 3 苗床 (x=55/75/140, y=40)
- **境界**: `wood_fence` × 2 (x=-120, y=145 / x=-60, y=188) 農地境界
- **動線**: `street_lamp` × 2 (x=-70, y=60/160) 歩道帯沿い、`asphalt` パッチ (x=110, y=100, 60×8) サイロ前小道

**アンビエント 3 セル**:

- **NW**: `kominka` (x=-100, y=22) + `laundry_on_wire` (x=-140, y=45) + `cat` (x=-170, y=35) — 農家的雰囲気
- **NE**: `house` (x=100, y=22) + `trash_bag` (x=160, y=55) 農具風 + `potted_plant` 苗床
- **SW**: 空き地 `grass` 大パッチ + `cat` (x=-150, y=155) + `wood_fence` 畔道

**取り巻き 3 パターン**:

- **facade 沿い** (y=22): `kominka` (NW) / `house` (NE) のみ、SE 側は田畑のため空白（焦点 grain_silo を立てる）
- **焦点囲み**: SE サイロ前 田の字 + `greenhouse` × 2 + `puddle_reflection` × 2
- **歩道沿い** (x=-70): `street_lamp` × 2、並木 `pine_tree` 西列 × 3 (x=-170, y=10/90/175)、東列 × 2 (x=170, y=30/150)

**人配置**: 農家主婦 × 1 (x=-150, y=155 SW)、農作業員 × 1 (x=140, y=145 SE 田)、歩道通行人 × 1 (x=-65, y=65)、`cat` × 2 (NW/SW)

**地面パッチ**:

- **基調**: `grass` 全面 (x=0, y=100, 360×200) — Act IV 田園
- **SE 焦点**: `dirt` 田の字 (x=-20 y=160, 130×80) + 中央列 dirt 細帯 × 2 (x=-40/0, y=115)
- **歩道帯**: `stone_pavement` 廃止、`grass` 全面で代替（郊外感）
- **道路パッチ**: `asphalt` サイロ前 (x=110, y=100, 60×8)

**道路**: `verticalRoads: [_AVE]` / `horizontalRoads: [_MID_HR, _HR(60, -180, -80, 'service')]`（農具小道は部分幅のみ）

**生活の痕跡 / スケール / 向き**:

- **痕跡**: NW `laundry_on_wire`、NE `trash_bag`、SW `cat`、SE `puddle_reflection` × 2 — 4 セル全 ✅
- **スケール**: 大 `grain_silo` ★ / 中 `kominka`/`house`/`greenhouse` × 2 / 小 `wood_fence` × 6・`potted_plant` × 3
- **向き**: `greenhouse` は南向き、`wood_fence` は田の字四方、`cat` は人の方向

**Handoff (Ch9 → Ch10)**:

- **並木**: `pine_tree` (x=-170, y=175) ↔ Ch10 y=15 一致（Ch8 から完全移行済）
- **電柱/電線**: `power_pole` (±178, 195) + `power_line` → Ch10
- **予兆**: SE サイロから NE 倉庫感を残し、Ch10 上段マージへの転調

**TS スケルトン:**

```tsx
export const CHUNK_9: Chunk = {
  y: 1600,
  buildings: [
    _B('grain_silo', 110, 118),
    _B('kominka', -100, 22),
    _B('house', 100, 22),
  ],
  furniture: [
    _F('greenhouse', 150, 135),
    _F('greenhouse', 150, 175),
    _F('wood_fence', 20, 140),
    _F('wood_fence', 20, 175),
    _F('wood_fence', -40, 140),
    _F('wood_fence', -40, 175),
    _F('puddle_reflection', -60, 110),
    _F('puddle_reflection', 60, 110),
    _F('laundry_on_wire', -140, 45),
    _F('cat', -170, 35),
    _F('cat', -150, 155),
    _F('trash_bag', 160, 55),
    _F('potted_plant', 55, 40),
    _F('potted_plant', 75, 40),
    _F('potted_plant', 140, 40),
    _F('wood_fence', -120, 145),
    _F('wood_fence', -60, 188),
    _F('power_pole', -178, 92),
    _F('power_pole', +178, 92),
    _F('power_pole', -178, 195),
    _F('power_pole', +178, 195),
    _F('power_line', -90, 195),
    _F('power_line', +90, 195),
    _F('pine_tree', -170, 10),
    _F('pine_tree', -170, 90),
    _F('pine_tree', -170, 175),
    _F('pine_tree', +170, 30),
    _F('pine_tree', +170, 150),
    _F('street_lamp', -70, 60),
    _F('street_lamp', -70, 160),
  ],
  humans: [
    _H(-150, 155),
    _H(140, 145),
    _H(-65, 65),
  ],
  grounds: [
    _G('grass', 0, 100, 360, 200),
    _G('dirt', -20, 160, 130, 80),
    _G('dirt', -40, 115, 20, 20),
    _G('dirt', 0, 115, 20, 20),
    _G('asphalt', 110, 100, 60, 8),
  ],
  roads: [..._SPINE_V, _MID_HR, _HR(60, -180, -80, 'service')],
};
```

### Ch10: 郊外倉庫と消防分署（NW+NE マージ）

**ヒーロー / アンビエント**: **NW+NE マージ = 大ヒーロー**（公共クラスター、基本形 C 3 連 + ランドマーク）/ SW・SE = アンビエント

**NW+NE 大ヒーロー 4 層**:

- **焦点**: `warehouse` (x=-100, y=22) + `fire_watchtower` (x=60, y=10) ★ ランドマーク + `police_station` (x=120, y=22)
- **取り巻き（焦点囲み）**: `flag_pole` (x=40, y=38) 消防、`bench` (x=140, y=42) 派出所前、`potted_plant` (x=155, y=35)、`vending` (x=-50, y=35)、`traffic_cone` × 3 (消防出動帯、x=55/80/100, y=48-52)
- **境界**: `cable_junction_box` (x=-140, y=48) 西端、`power_pole` (x=-60, y=42) + `power_line` (x=-80, y=42)
- **動線**: 消防サービス道 `_HR(65, 20, 180, 'service')` NE 出動ルート、`street_lamp` × 2 (x=-70, y=60/160)

**アンビエント 3 セル**:

- **SW**: 空き地 `grass` + `cable_junction_box` (x=-110, y=155) + `puddle_reflection` × 2 (x=-140/-40, y=160/170) + `traffic_cone` × 2 (x=-170/-80, y=145/155) — 疑風景
- **SE**: `kominka` (x=110, y=130) + `laundry_on_wire` (x=140, y=160) + `cat` (x=165, y=175) + `trash_bag` (x=150, y=188) — 最終居住感
- **NW**: 公共クラスターに飲み込まれ独立セルなし（アンビエントは 2 セル）

**取り巻き 3 パターン**:

- **facade 沿い** (y=22): `warehouse` / `police_station` のみ、中央 fire_watchtower で構造化
- **焦点囲み**: 上段公共クラスター周辺の `flag_pole` + `traffic_cone` × 3 + `bench` + `vending`
- **歩道沿い** (x=-70): `street_lamp` × 2、`pine_tree` 西列 × 3 (x=-170, y=15/100/185)、東列 × 2 (x=170, y=55/175)

**人配置**: 警官 × 1 (x=130, y=42 派出所前)、消防員 × 1 (x=80, y=48 traffic_cone 帯)、歩道通行人 × 1 (x=-65, y=70)

**地面パッチ**:

- **基調**: `asphalt` 上段 (x=-100/90, y=55, 160/180×60) 公共クラスター帯
- **NW+NE 焦点**: `asphalt` 公共敷地、`asphalt` 出動レーン (x=120, y=150, 140×20)
- **SW 痕跡**: `oil_stained_concrete` (x=-130, y=155, 60×30) 廃工場感
- **SE アンビエント**: `grass` (x=-90, y=160, 180×80) 空き地

**道路**: `verticalRoads: [_AVE]` / `horizontalRoads: [_MID_HR, _HR(65, 20, 180, 'service')]`（消防出動サービス道）

**生活の痕跡 / スケール / 向き**:

- **痕跡**: NW `cable_junction_box`、NE `flag_pole`、SW `puddle_reflection` × 2 + `cable_junction_box`、SE `laundry_on_wire` + `cat` + `trash_bag` — 4 セル全 ✅
- **スケール**: 大 `warehouse`/`fire_watchtower` ★ / 中 `police_station`/`kominka` / 小 `traffic_cone` × 5・`puddle_reflection` × 3
- **向き**: `bench` は派出所向き、`traffic_cone` は出動レーン整列、`cat` は人方向

**Handoff (Ch10 → Ch11)**:

- **並木**: `pine_tree` (x=-170, y=185) ↔ Ch11 y=15 一致
- **電柱/電線**: `power_pole` × 4 (±178, y=92/195) + `power_line` × 2 → Ch11
- **予兆**: 公共クラスター上段から踏切下段への転調、`puddle_reflection` × 3 が Ch11 夜ネオン路面の予告

**TS スケルトン:**

```tsx
export const CHUNK_10: Chunk = {
  y: 1800,
  buildings: [
    _B('warehouse', -100, 22),
    _B('police_station', 120, 22),
    _B('kominka', 110, 130),
  ],
  furniture: [
    _F('fire_watchtower', 60, 10),
    _F('flag_pole', 40, 38),
    _F('traffic_cone', 80, 48),
    _F('traffic_cone', 55, 52),
    _F('traffic_cone', 100, 52),
    _F('bench', 140, 42),
    _F('potted_plant', 155, 35),
    _F('power_pole', -60, 42),
    _F('power_line', -80, 42),
    _F('cable_junction_box', -140, 48),
    _F('vending', -50, 35),
    _F('puddle_reflection', -140, 160),
    _F('puddle_reflection', -40, 170),
    _F('puddle_reflection', 30, 165),
    _F('cable_junction_box', -110, 155),
    _F('traffic_cone', -170, 145),
    _F('traffic_cone', -80, 155),
    _F('laundry_on_wire', 140, 160),
    _F('cat', 165, 175),
    _F('trash_bag', 150, 188),
    _F('potted_plant', 85, 145),
    _F('potted_plant', 130, 145),
    _F('wood_fence', 50, 195),
    _F('power_pole', -178, 92),
    _F('power_pole', +178, 92),
    _F('power_pole', -178, 195),
    _F('power_pole', +178, 195),
    _F('power_line', -90, 195),
    _F('power_line', +90, 195),
    _F('pine_tree', -170, 15),
    _F('pine_tree', -170, 100),
    _F('pine_tree', -170, 185),
    _F('pine_tree', +170, 55),
    _F('pine_tree', +170, 175),
    _F('street_lamp', -70, 60),
    _F('street_lamp', -70, 160),
  ],
  humans: [
    _H(130, 42),
    _H(80, 48),
    _H(-65, 70),
  ],
  grounds: [
    _G('asphalt', -100, 55, 160, 60),
    _G('asphalt', 90, 55, 180, 60),
    _G('grass', -90, 160, 180, 80),
    _G('asphalt', 120, 150, 140, 20),
    _G('oil_stained_concrete', -130, 155, 60, 30),
  ],
  roads: [..._SPINE_V, _MID_HR, _HR(65, 20, 180, 'service')],
};
```

### Ch11: 踏切と夕鬼ネオン雲集（SW+SE マージ、Stage 2 接続）

**ヒーロー / アンビエント**: **SW+SE マージ = 大ヒーロー**（踏切 + 夜ネオン三連、基本形 A 全幅 + 3 連店舗）/ NW・NE = アンビエント（控えめ）

**SW+SE 大ヒーロー 4 層**:

- **焦点**: `railroad_crossing` (x=0, y=170 全幅横断) + `signal_tower` × 2 (x=±140, y=150) + `gas_station` (x=-100, y=118) + `convenience` (x=100, y=118) + `ramen` (x=0, y=118)
- **取り巻き（焦点囲み）**: `chouchin` × 2 (x=±12, y=132) ramen 前、`noren` (x=0, y=132) ramen 暖簾、`a_frame_sign` × 2 (x=-8/85, y=138) 店前看板、`vending` × 2 (x=80/120, y=132)、`bicycle_rack` (x=140, y=152)、`traffic_cone` × 多 (x=-130/-110/-80, y=138 + x=-40/+40, y=165) 踏切警告
- **境界**: `puddle_reflection` × 3 (x=-60/+60/0, y=185/180/192) 濡れた路面、`signal_tower` × 2 で avenue 完全分断
- **動線**: `street_lamp` × 2 黄美系光源、`railroad_crossing` 自体が動線の遮断要素 — Stage 2 接続点 y=200 は spine のみ渡る

**アンビエント 3 セル**:

- **NW**: 空き地 `grass` + `cat` + `puddle_reflection` — 控えめ
- **NE**: `kominka` (x=100, y=22) + `laundry_on_wire` — 最終居住感
- **マージ余地**: 上段は控えめでフィナーレ下段に視線集中

**取り巻き 3 パターン**:

- **facade 沿い** (y=22): `kominka` (NE) のみ、上段は焦点立てるため空白
- **焦点囲み**: ramen 前 `chouchin` × 2 + `noren` + `a_frame_sign` × 2、踏切前 `traffic_cone` × 多
- **歩道沿い** (x=-70): `street_lamp` × 2、Ch10 から `pine_tree` 連続、`power_pole` × 4 (±178, y=92/195)

**人配置**: ramen 客 × 2、convenience 客 × 1、踏切待ち × 2、歩道通行人 × 1、`cat` × 1 NW

**地面パッチ**:

- **基調**: `asphalt` 全面（夜の路面感）
- **SW+SE 焦点**: 店前 `concrete` (3 連店帯)、踏切 `dirt` (railroad_crossing 周辺)
- **痕跡**: `oil_stained_concrete` 小 (gas_station 前)、`puddle_reflection` × 3 で湿り気最高潮

**道路**: `verticalRoads: [_AVE]` / `horizontalRoads: [_MID_HR]`（railroad_crossing 自体が y=170 で avenue を分断するため他の水平道は追加しない）

**生活の痕跡 / スケール / 向き**:

- **痕跡**: NW `cat` + `puddle_reflection`、NE `laundry_on_wire`、SW gas_station 前 `oil_stained_concrete`、SE 踏切 `puddle_reflection` × 3 — 4 セル全 ✅
- **スケール**: 大 `railroad_crossing` 全幅 / 中 `gas_station`/`convenience`/`ramen`/`signal_tower` × 2 / 小 `chouchin` × 2・`traffic_cone` × 多
- **向き**: `signal_tower` は avenue 中央、`chouchin` は ramen 前、`a_frame_sign` は通行人方向、`puddle_reflection` は街灯反射

**Handoff (Ch11 → Stage 2 Ch0)**:

- **連続軸**: `pine_tree` 並木は Stage 1 で終了、Stage 2 は街灯ネオンが新連続軸
- **歩道**: `stone_pavement` 廃止 → Stage 2 では `asphalt` 主体
- **電柱/電線**: 4 隅で対 + `cable_junction_box` 増 (Stage 2 へ予告)
- **予兆**: `gas_station` + `convenience` + `ramen` ネオン三連 + `chouchin` 帯が Stage 2 駅前歓楽街への入口感

**TS スケルトン:**

```tsx
export const CHUNK_11: Chunk = {
  y: 2200,
  buildings: [
    _B('gas_station', -100, 118),
    _B('convenience', 100, 118),
    _B('ramen', 0, 118),
    _B('kominka', 100, 22),
  ],
  furniture: [
    _F('railroad_crossing', 0, 170),
    _F('signal_tower', -140, 150),
    _F('signal_tower', +140, 150),
    _F('traffic_cone', -40, 165),
    _F('traffic_cone', 40, 165),
    _F('puddle_reflection', -60, 185),
    _F('puddle_reflection', 60, 180),
    _F('puddle_reflection', 0, 192),
    _F('chouchin', -12, 132),
    _F('chouchin', 12, 132),
    _F('noren', 0, 132),
    _F('a_frame_sign', -8, 138),
    _F('vending', 80, 132),
    _F('vending', 120, 132),
    _F('bicycle_rack', 140, 152),
    _F('a_frame_sign', 85, 138),
    _F('traffic_cone', -130, 138),
    _F('traffic_cone', -110, 138),
    _F('traffic_cone', -80, 138),
    _F('vending', -150, 132),
    _F('cable_junction_box', -160, 152),
    _F('cat', -160, 45),
    _F('puddle_reflection', -130, 55),
    _F('laundry_on_wire', 140, 55),
    _F('trash_bag', 160, 75),
    _F('potted_plant', 80, 35),
    _F('potted_plant', 130, 35),
    _F('wood_fence', -40, 5),
    _F('power_pole', -178, 92),
    _F('power_pole', +178, 92),
    _F('power_pole', -178, 195),
    _F('power_pole', +178, 195),
    _F('power_line', -90, 195),
    _F('power_line', +90, 195),
    _F('pine_tree', -170, 15),
    _F('pine_tree', -170, 90),
    _F('pine_tree', +170, 40),
    _F('pine_tree', +170, 85),
    _F('street_lamp', -70, 60),
    _F('street_lamp', -70, 155),
  ],
  humans: [
    _H(-100, 138),
    _H(100, 138),
    _H(0, 138),
  ],
  grounds: [
    _G('grass', -90, 55, 180, 100),
    _G('asphalt', -100, 150, 160, 80),
    _G('asphalt', 100, 150, 160, 80),
    _G('oil_stained_concrete', -100, 140, 70, 30),
    _G('asphalt', 0, 180, 360, 40),
    _G('residential_tile', 110, 55, 140, 100),
  ],
  horizontalRoads: [_MID_HR],
  verticalRoads: [_AVE],
};
```

**Stage 2 への handoff:** `railroad_crossing` が y=170 で avenue を完全分断し、Stage 2 冒頭は踏切の向こう側から再開する。`puddle_reflection` の夜ネオン反射と `chouchin` の黄美は Stage 2（夜街）のトーンを予告する。Stage 1 の 全 `cat` 総数算入: 8 匹以上（最終チェック）。

---

<aside>
🎉

**Stage 1 詳細フェーズ完成**

Ch0〜Ch11 の全 12 チャンクについて、ヒーロー/アンビエント割当・4 層構造・取り巻き 3 パターン・人配置・地面パッチ・生活の痕跡・Handoff 4 項目・TS スケルトン が揃った。実装者は各チャンクの TS スケルトンを `src/stages.ts` の対応チャンクに置換し、型チェックで FurnitureType / BuildingSize / GroundType union とすり合わせること。

</aside>

---

# §8 変更履歴

- **v6.0 (詳細フェーズ完成版)**: §9 詳細フェーズを全 12 チャンク分追加（Ch0〜Ch11）。Act ヘッダー（Act I/II/III/IV）を全て挨入。ステージ全体ジグザグ流れを紫 callout で明示。各チャンクにはヒーロー/アンビエント割当・焦点 4 層構造・取り巻き 3 パターン・人配置・地面パッチ・追加道路・生活の痕跡・スケール階層・向きの意図・Handoff 4 項目・`ChunkTemplate` の TS スケルトン（raw ヘルパー `_B/_F/_H/_G/_HR/_VR/_SPINE_V/_MID_HR/_TOP_HR` を使用）を液載。タイトルを v5.5 → v6.0 にバンプ。
- **v1-v4**: 誤ったステージコンセプト（鳥居・城下町・祭り・五重塔・駅前広場・ダウンタウン等）に基づく指示書。`src/stages.ts` を未確認のまま創作したもの。**全面撤回**。
- **v5**: `src/stages.ts` の `STAGE_1_TEMPLATES` を確認し、実際の 4 Acts 構造（住宅街→生活商店→商店街→街はずれ）を前提に方針レベルで書き直し。
- **v5.1**: v5 に対し、詳細フェーズで構造判断を発明しなくて済むよう以下を追加 — §3.5 セル分割の基本グリッド（2×2、マージルール、密度配分）/ §4.5 建物タイプ別取り巻き辞書 / §4.6 人流配置ルール / §5 道路タイプ表の完成（avenue・main/top horizontal・部分幅小道の行）/ §6 チャンク境界 handoff 記述フォーマット。
- **v5.2**: v5.1 に対し、ミニチュア整合性を全 12 チャンクで担保するため以下を追加 — §4.7 取り巻き配置の 3 パターン（facade 沿い / 焦点取り囲み / 歩道沿い）/ §5 末尾 地面パッチの配分原則 / §6 セル境界の連続性。
- **v5.3**: 「詰め込み回避」の基準を明確化 — 建物は上限（焦点役割のあるものだけ、意味のない背景埋め禁止）、家具は下限（密度はむしろ家具で作る、上限緩め）。§4 冒頭に「建物と家具の扱いの違い」callout 追加、§3.5 セル密度配分と §6 精巧さ最低ラインを再定義。
- **v5.4**: ミニチュア精度の上限を引き上げる追加 — §3.6 ヒーロー/アンビエント階層（チャンク内 1-2 セルがヒーロー、残りはアンビエント、隣接禁止、Stage 全体ジグザグ）/ §3 各 Act に時間帯を明示（朝 / 昼 / 午後 / 夕方夜、furniture 選択の方向付け）/ §4.8 スケール階層（大中小 3 段階比 1:2-3:10-20）・生活の痕跡（laundry / 傾き bicycle / fallen_leaves / mailbox flag 等、各セル 1 個以上）・向きの意図（ベンチは焦点向き / 自販機は壁背 / 物干しは南向き 等）/ §6 精巧さ最低ラインに痕跡・ヒーロー構成・スケール階層・孤立電柱禁止・向き一貫性を追加。
- **v5.5**: 全体整合性チェックによる破綻修正と補完 — (1) §1 4 Acts 表に Act I/III 行を補完（欠落していた）/ (2) §3 Act II 時間帯を「朝〜昼」に拡張（Ch3 「朝のカフェテラス」との整合）/ (3) §3.6 マージチャンク扱いを明確化（アンビエントセルなしの例外 + Act 境界 Ch5→Ch6→Ch7 の 3 連続大ヒーロー許容）/ (4) §5 Act ごとの道路性格表に Act I/III 行を補完（欠落していた）/ (5) §6 精巧さ最低ラインのヒーロー構成にマージチャンク例外を明記 / (6) §2 階層 1 に §3.6 の参照を追加。詳細配置は次フェーズ。
- **v6.1**: 整合性アップデート — §6.5 新設（ドキュメント中の型名と実 union の置換表 / `residential_wood` `cottage_2f` `bathhouse` `laundry_on_wire` `vending_machine` 等の置換ルール）。§4.8「向きの意図」改訂（`FurnitureItem` に `facing` フィールドが無い制約整理 + エンジン改修 TODO 明記）。Ch1 を見本チャンクに昇格。道路指示を main ブランチ準拠の `horizontalRoads` / `verticalRoads` 形式に統一し、`_AVE` 中心 + チャンク固有道路の方針に改訂。
- **v6.2**: 道路設計ポリシー改訂 — (1) **固定する道路を中央 `_AVE`（x=0 縦軸）のみ** に縮小。`_MID_HR` を含む横道路と、`_AVE` 以外の縦道路はすべてチャンクのコンセプトに沿って都度設計する方針へ変更。 (2) §5 を全面書き直し（「全チャンクに `_MID_HR` を必ず置く」前提を撤廃 / 設計手順「コンセプト → 動線 → 焦点境界 → 道路セグメント」を明文化 / Act ごとの道路性格・チャンク別参考道路を「例示」化 / 横道路ゼロ `horizontalRoads: []` を正当な選択として認める）。 (3) §1 / §2 / §3.5 / §3.6 / §4 で `_MID_HR` を固定として扱っていた記述を「論理境界 y=100」または「採用しているチャンクのみ」に修正。 (4) §6 ヘルパー説明を新方針に整合。 (5) §6.5 実装手順を新方針に整合（コンセプトから道路を再設計するフローに変更）。 (6) §9 冒頭の「読み方」box に v6.2 注意書きを追加し、各チャンクの道路記述・スケルトンの `horizontalRoads` / `verticalRoads` を **v6.1 当時の例示** として読むこと、実装時はチャンクのコンセプトから再設計してよいことを明記。
- **v6.3（本版）**: SemanticCluster 反映 — 配置の意味付けレイヤーを実装。(1) `RawChunkBody.clusters?: SemanticCluster[]` をオプショナル追加（既存 12 チャンク無変更で後方互換 100%）。(2) `SemanticCluster` 型は `{ id, role: 'hero'|'ambient', cell, focal, companions?, boundary?, access?, livingTrace? }` で §3.6 / §4 を機械可読化。Ref はインデックス参照（`{ kind: 'b'|'f', i: number }`）。(3) Stage 1 全 12 チャンク（Ch0〜Ch11）に 1 hero + 0-3 ambient のクラスタを宣言。指示書 §3.6 のジグザグ流れがコードに反映。(4) `tools/check-placement.ts` に 9 ルール (L0-L9) のリンタを実装。`npm run lint:placement` で行進・対称・孤立電柱・facade 線逸脱を機械検出。(5) Ch1 児童公園をパイロットとして IIFE + `$B/$F/_CLUSTER` 記法で書き換え、見本実装に。鏡像対称 7 件を §6 「2-5px ずらす」原則で修正済み。(6) `particles.spawnAmbient(x, y, type)` を追加し、各 hero focal 周辺で常時 ambient particle（蒸気・桜・水・砂塵・ホタル）を低レート emit。focal 種別から自動派生（`kominka`→steam、`koi_pond`→water、`sakura_tree`→sakura など）。(7) §6.6 を新設し、SemanticCluster の使い方・リンタルール・指示書原則との対応表を文書化。